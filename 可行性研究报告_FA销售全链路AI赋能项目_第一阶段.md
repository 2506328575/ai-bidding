# 可行性研究报告

## FA 销售全链路 AI 赋能项目 — 第一阶段：售前初步闭环

| 项目 | 内容 |
|------|------|
| 文档版本 | v1.3 |
| 编制日期 | 2026-06-07（v1.3 更新技术栈） |
| 项目状态 | 可行性研究阶段 |
| 目标上线 | 2026 年第四季度 |

---

## 目录

1. [项目概述](#1-项目概述)
2. [技术架构](#2-技术架构)
3. [模块逐一分析](#3-模块逐一分析)
   - 3.1 技术问答
   - 3.2 需求梳理
   - 3.3 方案生成
   - 3.4 标书撰写
   - 3.5 政策打通
4. [技术选型汇总](#4-技术选型汇总)
5. [分批迭代计划](#5-分批迭代计划)
6. [风险矩阵](#6-风险矩阵)
7. [资源估算](#7-资源估算)
8. [可行性结论](#8-可行性结论)

---

## 1. 项目概述

### 1.1 项目背景

本项目旨在构建一套面向 FA（Fang An / 方案）销售团队的 AI 赋能系统，覆盖售前全链路的核心环节。第一阶段聚焦于 **售前初步闭环**，目标在 2026 年第四季度完成上线。

### 1.2 项目目标

通过 AI 技术实现售前工作的智能化升级：

| 目标 | 说明 |
|------|------|
| 提效 | 减少 FA 在技术问答、需求梳理、方案撰写中的重复性工作 |
| 标准化 | 保证输出方案和标书的质量一致性，沉淀历史经验 |
| 知识管理 | 将分散的 FA 案例、产品知识、政策法规系统化管理 |

### 1.3 第一阶段范围 — 五大模块

| 编号 | 模块 | 一句话描述 |
|------|------|-----------|
| M1 | 技术问答 | 售前产品&项目技术问题智能解答，FA 案例快速检索 |
| M2 | 需求梳理 | 进行深度需求挖掘，结构化拆分整理客户需求 |
| M3 | 方案生成 | 根据客户需求自动化生成定制化售前解决方案 PPT |
| M4 | 标书撰写 | 标书智能撰稿、格式校验、历史标书素材复用 |
| M5 | 政策打通 | 接入全国各省市社保、公积金属地政策知识库 |

---

## 2. 技术架构

### 2.1 技术栈：FastAPI-Amis-Admin

**后端：** FastAPI（Python 异步 Web 框架）

**选用理由：**
- 高性能异步架构：全栈异步支持，吞吐量比同步框架高 3-5 倍
- 自动生成 OpenAPI 文档：Swagger UI / ReDoc 零配置可用
- AI/LLM 生态天然匹配：Python 是 AI 开发的第一语言，LangChain / LlamaIndex / Milvus SDK 等全部原生 Python
- 数据验证强类型：Pydantic v2 提供类型安全 + JSON Schema 自动推导
- 低代码后台：Amis JSON 配置化前端，零前端代码生成 CRUD 界面

**前端：** Baidu Amis（低代码前端框架）

**选用理由：**
- JSON 配置驱动：声明式定义管理页面，代码量减少 95%
- 内置 60+ 组件：表格/表单/图表/文件上传一应俱全
- 浏览器端渲染：后端只输出标准 JSON，前后端自然分离
- 与 FastAPI 深度集成：`fastapi-amis-admin` 提供 ModelAdmin 模式，三行代码生成数据管理页面

**数据库 ORM：** SQLModel + SQLAlchemy

**选用理由：**
- SQLModel 融合 Pydantic + SQLAlchemy：一个类同时是数据库模型和 API Schema
- 支持同步/异步双模式
- 多数据库适配（SQLite 开发 / MySQL 生产）

### 2.2 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          AI 招投标系统 — 售前闭环                        │
│                              (2026 Q4 上线)                              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌───────────────────────────────────────────────────────────────┐      │
│  │                      前端层                                    │      │
│  │  ┌──────────────────────┐  ┌────────────────────────┐        │      │
│  │  │  Amis 管理后台        │  │  Vue 3 用户交互页面     │        │      │
│  │  │  (JSON配置/CRUD表格)  │  │  (对话助手/方案编辑)    │        │      │
│  │  │  · 模板管理           │  │  · AI 对话问答          │        │      │
│  │  │  · Prompt 管理        │  │  · 方案预览编辑         │        │      │
│  │  │  · 政策库维护         │  │  · 标书在线编辑         │        │      │
│  │  └──────────────────────┘  └────────────────────────┘        │      │
│  └──────────────────────────────────────────────────────────────┘      │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────┐      │
│  │                    FastAPI 后端 (Python)                      │      │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │      │
│  │  │ 技术问答  │ │ 需求梳理  │ │ 方案生成  │ │ 标书撰写  │       │      │
│  │  │  Router  │ │  Router  │ │  Router  │ │  Router  │       │      │
│  │  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘       │      │
│  └───────┼────────────┼────────────┼────────────┼──────────────┘      │
│          │            │            │            │                      │
│  ┌───────┴────────────┴────────────┴────────────┴──────────────┐      │
│  │                    AI 编排层 (核心)                           │      │
│  │  · LLM Service (httpx 异步调用)   · Prompt Manager           │      │
│  │  · RAG Engine (Milvus SDK)        · Template Filler          │      │
│  │  · Intent Classifier              · Markdown→Word Renderer   │      │
│  └──────────────────────────────────────────────────────────────┘      │
│                                                                          │
│  ┌────────────────────────────┬────────────────────────────┐            │
│  │      知识库层              │      业务数据层             │            │
│  │  ┌──────────────────┐     │  ┌──────────────────────┐  │            │
│  │  │  Milvus 向量数据库 │     │  │ MySQL / PostgreSQL    │  │            │
│  │  │  · FA 案例库       │     │  │ (SQLModel + SQLAlchemy)│  │            │
│  │  │  · 产品文档库      │     │  │  · 用户/权限/项目     │  │            │
│  │  │  · 政策法规库      │     │  │  · 模板元数据         │  │            │
│  │  │  · 历史标书素材库   │     │  │  · 审计日志           │  │            │
│  │  └──────────────────┘     │  └──────────────────────┘  │            │
│  └────────────────────────────┴────────────────────────────┘            │
│                                                                          │
│  ┌───────────────────────────────────────────────────────────────┐      │
│  │                    FastAPI 底座组件                             │      │
│  │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐    │      │
│  │  │ RBAC │ │Redis│ │APSched│ │文档│ │Milvus│ │Word│    │      │
│  │  │认证授权│ │缓存│ │uler│ │自动生成│ │SDK│ │渲染│    │      │
│  │  └──────┘ └──────┘ └──────┘ └──────┘ └──────┘ └──────┘    │      │
│  └───────────────────────────────────────────────────────────────┘      │
│                                                                          │
│  ┌───────────────────────────────────────────────────────────────┐      │
│  │                    外部依赖                                    │      │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐        │      │
│  │  │ LLM API  │ │  Milvus  │ │LibreOffice│ │ 对象存储  │        │      │
│  │  │(DeepSeek │ │(向量检索) │ │(格式转换)  │ │(MinIO/S3)│        │      │
│  │  │  /Qwen)  │ │          │ │          │ │(模板/素材)│        │      │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘        │      │
│  └───────────────────────────────────────────────────────────────┘      │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.3 关键架构决策

| 决策项 | 选择 | 理由 |
|--------|------|------|
| 后端框架 | FastAPI (Python) | 异步高性能；AI/LLM 生态原生 Python；Pydantic 强类型 |
| 前端框架 | Amis（管理后台）+ Vue 3（用户交互页） | Amis JSON 配置化零前端代码生成 CRUD；Vue 3 处理对话等复杂交互 |
| ORM | SQLModel + SQLAlchemy | Pydantic + SQLAlchemy 融合，同步/异步双模式 |
| AI 编排 | 轻量自研封装（httpx 异步调用） | 仅 3-4 个场景，Python 原生 AI 生态不需要 Java 级框架 |
| 向量数据库 | Milvus（独立实例，pymilvus SDK） | Python SDK 原生支持，扩展性好、混合搜索能力强 |
| 业务数据库 | MySQL / PostgreSQL | SQLModel 多数据库适配 |
| AI 访问数据 | RAG（向量检索）+ NL2SQL（结构化查询） | 知识库检索 + 实时数据库查询，覆盖两种场景 |
| 模板引擎 | python-docx + lxml | Python 原生，格式保留能力强 |
| 格式预览 | LibreOffice + JODConverter | PPTX/DOCX → PDF，浏览器可直接查看 |
| 模板管理 | Amis Admin 文件管理 + 自定义上传 | 支持用户上传/选择/版本管理 |

### 2.4 数据架构：AI 知识库与业务数据库分离

```
┌──────────────────────┐     ┌──────────────────────┐
│    AI 知识库 (Milvus)  │     │  业务数据库 (MySQL)     │
├──────────────────────┤     ├──────────────────────┤
│ FA 案例库 (向量化)     │     │ 用户/角色/权限         │
│ 产品技术文档 (向量化)   │     │ 客户/商机/项目         │
│ 政策法规 (向量+结构化) │     │ 需求记录/结构化数据    │
│ 历史标书素材 (向量化)  │     │ 模板元数据             │
│                      │     │ 操作日志/审计          │
└──────────────────────┘     └──────────────────────┘

AI 通过两种方式访问数据：
① 向量检索（RAG）→ 知识库中的非结构化/半结构化内容
② NL2SQL（自然语言转 SQL）→ 实时查询业务数据库中的结构化数据
```

### 2.5 Python 模块结构（FastAPI 规范）

```
backend/
├── main.py                         # FastAPI 入口 + AdminSite 挂载
├── core/
│   ├── settings.py                 # 全局配置（DB/LLM/Milvus）
│   └── admin.py                    # AdminSite 注册中心
├── apps/
│   ├── qa/                         # M1 技术问答
│   │   ├── models.py               #   SQLModel 数据模型
│   │   ├── schemas.py              #   Pydantic Schema
│   │   ├── apis.py                 #   FastAPI Router
│   │   ├── services/
│   │   │   ├── llm_client.py       #     LLM API 调用 (httpx)
│   │   │   ├── intent.py           #     意图分类
│   │   │   └── rag.py              #     RAG 检索 (pymilvus)
│   │   └── admin.py                #   Amis Admin 管理页
│   ├── requirement/                # M2 需求梳理
│   ├── proposal/                   # M3 方案生成
│   │   ├── apis.py
│   │   └── services/
│   │       ├── outline.py          #     大纲生成
│   │       └── docx_render.py      #     Word 渲染 (python-docx)
│   ├── bid/                        # M4 标书撰写
│   └── policy/                     # M5 政策打通
├── prompts/                        # Prompt 模板（YAML/JSON）
└── templates/                      # Word 模板文件
```

### 2.6 Prompt 管理系统

#### 为什么需要

Prompt（提示词）是 AI 模块的核心输入，直接影响所有 LLM 调用的输出质量。Prompt 的迭代频率远高于业务代码——调优周期以小时计，而传统发版以天/周计。

**核心原则：Prompt 与代码分离。** 代码里只写 key 引用，不写 prompt 原文。

```
❌ 错误做法：
  String prompt = "你是一个售前助手..." + userName + "...";
                    ↑ 改一句话 = 改代码 = 编译 = 打包 = 部署

✅ 正确做法：
  String prompt = promptManager.render("intent.classify", params);
                    ↑ 改一句话 = UPDATE 数据库 = 立即生效
```

#### 架构设计

```
┌────────────────────────────────────────────────────────────┐
│                    Prompt 管理系统                          │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ┌──────────────────────────────────────────────────┐     │
│  │  存储层                                           │     │
│  │                                                    │     │
│  │  ┌─────────────────┐     ┌──────────────────┐    │     │
│  │  │  MySQL (持久化)   │     │  Redis (缓存)     │    │     │
│  │  │  ──────────────  │     │  ──────────────  │    │     │
│  │  │  prompt_template  │ ←──→  prompt_cache     │    │     │
│  │  │  表               │     │  (Key-Value)     │    │     │
│  │  └─────────────────┘     └──────────────────┘    │     │
│  └──────────────────────────────────────────────────┘     │
│                                                            │
│  ┌──────────────────────────────────────────────────┐     │
│  │  Service 层                                      │     │
│  │                                                    │     │
│  │  PromptTemplateService                             │     │
│  │  ├── get(key, version)     — 读取原始模板          │     │
│  │  ├── render(key, params)   — 填充 {{占位符}}      │     │
│  │  ├── getActiveVersion(key) — 当前生效版本          │     │
│  │  └── publish(key, version) — 发布/回滚             │     │
│  └──────────────────────────────────────────────────┘     │
│                                                            │
│  ┌──────────────────────────────────────────────────┐     │
│  │  Amis Admin 管理后台                              │     │
│  │                                                    │     │
│  │  ┌──────────────┐ ┌──────────────┐ ┌──────────┐ │     │
│  │  │Prompt 列表   │ │ 在线编辑      │ │ 版本管理  │ │     │
│  │  │· 搜索/筛选   │ │· 语法高亮    │ │· 版本对比 │ │     │
│  │  │· 状态标签    │ │· 变量提示    │ │· 一键回滚 │ │     │
│  │  └──────────────┘ └──────────────┘ └──────────┘ │     │
│  └──────────────────────────────────────────────────┘     │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

#### 数据库设计

```sql
CREATE TABLE prompt_template (
    id          BIGINT AUTO_INCREMENT PRIMARY KEY,
    `key`       VARCHAR(100) NOT NULL COMMENT '模板标识，如 intent.classify',
    version     INT NOT NULL COMMENT '版本号',
    content     TEXT NOT NULL COMMENT 'Prompt 模板内容（含 {{占位符}}）',
    description VARCHAR(500) COMMENT '用途说明',
    status      TINYINT NOT NULL DEFAULT 0 COMMENT '0=草稿, 1=生产中, 2=历史',
    created_by  VARCHAR(50),
    created_at  DATETIME NOT NULL,
    updated_at  DATETIME NOT NULL,
    UNIQUE KEY uk_key_version (`key`, version)
) COMMENT 'Prompt 模板表';
```

#### 第一阶段 Prompt 清单（初始版本）

| Key | 用途 | 说明 |
|-----|------|------|
| `intent.classify` | 意图分类 | 判断用户问题是产品咨询/案例检索/政策问题/数据查询 |
| `qa.rag.answer` | 技术问答回答 | 结合 RAG 检索结果生成回答，标注来源 |
| `qa.nl2sql` | NL2SQL 生成 | 将自然语言问题转为 SQL 查询语句 |
| `requirement.collect` | 需求引导采集 | AI 引导用户补充关键需求信息 |
| `requirement.structure` | 需求结构化 | 将对话内容转为结构化 JSON |
| `proposal.outline` | 方案大纲规划 | 根据需求生成方案目录结构 |
| `proposal.section` | 方案单节撰写 | 生成方案中某一章节的内容 |
| `bid.extract` | 招标需求提取 | 从招标文件中提取关键要求和评分标准 |
| `bid.write` | 标书逐条响应 | 针对招标要求撰写对应内容 |
| `policy.analyze` | 政策抓取解析 | 从政府公告中提取结构化政策信息 |

> **注意：** 以上 Prompt 在 Demo 阶段可先放在 YAML 配置文件（`prompts/` 目录）中，但要预留好数据库表结构（SQLModel），上线前迁移到数据库管理。

#### 变更流程

```
修改 Prompt 的标准流程：

① 运营/FA 团队在管理后台 → 编辑 Prompt → 保存为草稿（新版本）
② 在管理后台进行版本对比，确认变更内容
③ 点击"发布" → 状态更新为"生产中"
④ 系统自动清除该 key 的 Redis 缓存
⑤ 下一次调用自动加载新版本 Prompt
⑥ 如有问题 → 管理后台"回滚" → 恢复上一版本

整个过程不需要发版，不需要重启服务。
```

---

## 3. 模块逐一分析

### 3.1 技术问答 (M1)

#### 功能描述

售前产品&项目技术问题智能解答，FA 案例快速检索。

#### 工作流程

```
用户提问（自然语言）
    │
    ▼
┌──────────────────────────────────────────────────┐
│ 第一步：意图识别                                 │
│                                                  │
│  实现方式：关键词命中（快路径） + LLM 分类（慢路径）  │
│                                                  │
│  ┌────────────────────────────────────────┐      │
│  │ 关键词快速命中（毫秒级，不走 LLM）：      │      │
│  │  包含 "社保/公积金/基数/比例" → POLICY  │      │
│  │  包含 "案例/做过/类似/行业"   → CASE    │      │
│  │  包含 "合同/金额/项目/多少"  → DATA     │      │
│  │  以上都没命中 → 走 LLM 分类             │      │
│  └────────────────────────────────────────┘      │
│                                                  │
│  输出：意图标签                                   │
│  ─────────────────                               │
│  PRODUCT_QUERY  — 产品技术咨询（走 RAG）          │
│  CASE_RETRIEVAL — FA 案例检索（走 RAG）           │
│  POLICY_QUERY  — 政策法规问题（走 RAG）          │
│  DATA_QUERY    — 业务数据查询（走 NL2SQL）      │
│  GREETING      — 打招呼/闲聊（直接回复）         │
└──────────────────────┬───────────────────────────┘
                       │
                       ▼
          ┌────────────┴────────────┐
          │                         │
          ▼                         ▼
┌──────────────────┐    ┌──────────────────────┐
│  ② RAG 检索路径   │    │  ② NL2SQL 查询路径    │
│  (非结构化数据)    │    │  (结构化业务数据)      │
│                  │    │                      │
│  用户问的是：      │    │  用户问的是：          │
│  "有制造业案例吗"  │    │  "去年签了多少合同"    │
│  "系统支持信创吗"  │    │  "我名下几个项目"      │
│  "广州社保基数"    │    │  "技术标中标率多少"   │
│                  │    │                      │
│  ────────────    │    │  ────────────        │
│  ① 问题 Embedding│    │  ① LLM 将问题转 SQL  │
│    向量化         │    │    "去年签了多少合同"  │
│                  │    │    → SELECT COUNT(*)  │
│  ② Milvus 相似度  │    │      FROM contract   │
│    检索           │    │      WHERE sign_date │
│  → 返回 Top-K     │    │      BETWEEN ...     │
│    相关文档片段    │    │                      │
│                  │    │  ② Python 执行 SQL     │
│  ③ LLM 基于检索   │    │     （有限权限）      │
│    结果生成回答    │    │                      │
│    + 标注引用来源  │    │  ③ LLM 将结果转为    │
│                  │    │     自然语言回答        │
└────────┬─────────┘    └──────────┬───────────┘
         │                         │
         └──────────┬──────────────┘
                    ▼
┌──────────────────────────────────────────────────┐
│ 第三步：LLM 生成最终回答                        │
│                                                  │
│  ┌────────────────────────────────────────┐      │
│  │ 统一经过 LLM 转成自然语言（两种路径都要）  │      │
│  │                                         │      │
│  │ RAG 路径 → LLM 基于搜到的文档片段回答     │      │
│  │    "根据案例库资料，我们曾为 XX 制造      │      │
│  │     集团实施过 ERP 项目（来源：案例 #12）" │      │
│  │                                         │      │
│  │ NL2SQL 路径 → LLM 基于查询结果回答       │      │
│  │    "2025 年广东区域共签订 47 份合同"     │      │
│  └────────────────────────────────────────┘      │
│                                                  │
│  回答中必须包含来源标注，让用户知道信息从哪来的   │
└──────────────────────────────────────────────────┘
```

#### RAG 与 NL2SQL 对比

| 维度 | RAG（检索增强生成） | NL2SQL（自然语言转 SQL） |
|------|--------------------|------------------------|
| **解决什么问题** | AI 不知道的知识 → 搜文档后回答 | 数据库里的精确数据 → AI 转成 SQL 查询 |
| **数据来源** | Milvus 向量库（FA 案例/产品文档/政策法规） | MySQL 业务数据库（合同/项目/商机） |
| **数据类型** | 非结构化文本（文档/案例/政策文章） | 结构化数据（数字/状态/金额/日期） |
| **结果性质** | 语义匹配，近似答案（最相关的案例/条款） | 精确计算，确定答案（精确数字/统计结果） |
| **典型问题** | "有没有制造业的案例？" "社保基数多少？" | "去年签了多少合同？" "我名下几个项目？" |
| **AI 是否可替代** | ❌ 不能，这是 AI 的核心价值——理解语义 | ⚠️ 可以，但 AI 让非技术用户也能查数据 |
| **安全性关注点** | 低——只读文档，不涉及数据修改 | 中——SQL 执行需权限控制，只读+写安全校验 |
| **实现复杂度** | 中——需要 Milvus + Embedding 链路 | 中——需要 SQL 白名单 + 结果校验 |

**不分流会怎样？**

```
用户问："去年广东区域签了多少合同？"

→ 如果走 RAG（错误路径）：
  LLM 没有合同数据，会瞎编一个数字"大约 30 份左右"
  → 这是 AI 幻觉，数据不可信

→ 如果走 NL2SQL（正确路径）：
  LLM 生成 SQL → 执行查询 → 返回精确结果 "47 份"
  → 数据可信，可追溯
```

**分流的关键：意图识别必须准确。** 这就是为什么需要意图分类（关键词 + LLM 双保险）。

#### 可行性评估

#### 前提条件

- FA 团队需提供案例文档（Word/PDF/PPT/Markdown，建议 20+ 完整案例）
- 产品技术文档需整理（产品白皮书、技术方案说明书等）
- 确定 LLM API 服务商（DeepSeek / Qwen / GLM）

#### 关键风险

| 风险 | 级别 | 缓解措施 |
|------|------|---------|
| 知识库初始无数据 | 🔴 高 | 要求客户在启动前准备基础文档 |
| 回答质量不可控（幻觉） | 🟡 中 | RAG 强制引用来源，回答可溯源 |
| NL2SQL 准确率 | 🟡 中 | 限定查询范围（预定义表结构），复杂查询人工兜底 |

---

### 3.2 需求梳理 (M2)

#### 功能描述

AI 引导式对话，对客户需求进行深度挖掘和结构化拆分整理。

#### 工作流程

```
用户初始需求描述（一句话也可启动）
    │
    ▼
┌──────────────────────────────────────────────────────────┐
│ 阶段①：状态机驱动 AI 引导采集                              │
│                                                           │
│  维度列表（按 sort_order 排序，支持管理端配置）            │
│  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐      │
│  │ 行业 │→│ 规模 │→│ 痛点 │→│ 预算 │→│ 时限 │→ ...     │
│  └──┬───┘  └──┬───┘  └──┬───┘  └──┬───┘  └──┬───┘      │
│     │         │         │         │         │             │
│     ▼         ▼         ▼         ▼         ▼             │
│  [已填]   [已填]   [待追问]  [未开始]  [未开始]           │
│                      │                                    │
│                      ▼                                    │
│               当前追问维度：痛点                          │
│               AI 聚焦此维度单一提问                       │
│               用户回答 → 标记已填 → 进入下一维度          │
└──────────────────────┬───────────────────────────────────┘
                       │
┌──────────────────────▼───────────────────────────────────┐
│ 阶段②：结构化输出                                         │
│ - 所有必填维度（已填 or 待补充）→ 触发结构化               │
│ - 从完整对话中提取 JSON（固定字段 + 灵活字段）+ 摘要       │
│ - 固定字段不可缺，灵活字段由管理端配置决定                 │
└──────────────────────┬───────────────────────────────────┘
                       │
┌──────────────────────▼───────────────────────────────────┐
│ 阶段③：关联推荐                                           │
│ - 基于需求画像向量 + 元数据过滤 → 从 Milvus 检索匹配案例  │
│ - 推荐适配的技术方案类型                                   │
│ - 整个结构化 JSON 作为方案生成 (M3) 的直接输入             │
└──────────────────────────────────────────────────────────┘
```

#### AI 追问机制 — 状态机驱动，每次只追问一个维度

AI 不是自由发挥提问，而是严格的维度驱动状态机。

**追问策略（编码在 Prompt + Session 状态中）：**

| 规则 | 说明 |
|------|------|
| 每次只追问一个维度 | 禁止一回合问多个维度（"顺便问一下...你们预算..." ❌） |
| 优先追问状态为"待追问"的维度 | 后端组装 Prompt 时注入 session 状态表 |
| 用户未回答即跳过 | 再追问同一维度一次 → 如仍未回答 → 标记"待补充"，继续下一个维度 |
| 用户主动补充了其他维度 | LLM 在回答中输出标记 `[FILLED:维度名]` → 后端解析后自动更新状态 |
| 所有必填维度完成 | 触发结构化输出阶段，不再追问 |

**每次调用 LLM 实际使用的 Prompt 状态表（requirement.collect）：**

```
你是售前需求分析助手。请根据当前客户描述的内容进行提问，以便完善需求信息。

## 当前需求采集状态
| 维度 | 状态 | 已有内容 |
|------|------|---------|
| 行业 | ✅已填 | 制造业 |
| 规模 | ✅已填 | 200-500万 |
| 核心痛点 | ⚠️待追问 | （空）  ← 当前聚焦维度
| 预算范围 | ⏳未开始 | （空） |
| 交付时限 | ⏳未开始 | （空） |

## 追问规则（严格遵守）
1. 每次只追问【当前聚焦维度】，严禁一次提多个维度的问题
2. 先总结已了解的用户痛点，再用自然语气引入当前维度
   "了解到您这边主要关注的是制造业ERP系统升级，为了帮您匹配合适的方案，想了解一下..."
3. 追问2次若用户仍未对该维度作答，则标记"待补充"继续下一维度
4. 用户在回答中顺便补充了其他维度时，在回复末尾用 [FILLED:维度名] 格式标注

💡 用户刚刚说：{user_latest_input}
```

> **关键设计：** 维度状态表由后端在每次调用前组装填充，不是 LLM 自己记忆。每个维度推进完全在 Session 层控制，代码里就能知道"用户填了几个维度、现在该问什么"。

---

#### 追问偏移与回正机制

AI 在多轮对话中可能出现两种偏移。两种都设计了回正策略。

**第一种：主题偏移**

| 表现 | AI 从"追问痛点"偏到了"介绍公司发展史" |
|------|--------------------------------------|
| 成因 | 提示词冲突（上下文残留了问答模式的其他指令），或用户反问题干扰了方向 |
| 危险 | 对话不知不觉滑出需求采集流程，用户 5 分钟后发现还在聊产品功能 |

**第二种：范围偏移**

| 表现 | AI 凭空追问一个不在维度表里的细碎问题（"服务器用什么操作系统？"） |
|------|----------------------------------------------------------------|
| 成因 | LLM 对"深入了解客户需求"的过度脑补，认为多问总比少问好 |
| 危险 | 采集了不需要的数据，耗费用户耐心，后面结构化输出时包含无用字段 |

**三层防御回正机制：**

```
第一层：Prompt 内置状态表约束（预防，每次调用都注入）
  → 维度表就是护栏，告诉 AI "你只许追问当前维度"
  → 多数偏移在这一层就能避免

第二层：后端每轮后嗅探（检测，中间件拦截）
  → 每轮 LLM 回答后，后端调一次 LLM 做意图嗅探（成本极低）
        判断：本轮输出是否围绕当前维度在追问？
        判断：本轮输出是否引入了不在维度表里的异常字段？
  → 发现偏了 → 下一轮 Prompt 强制注入回正指令
        "⚠️ 上一轮你可能偏离了追问方向。请回到维度「核心痛点」，继续追问。"

第三层：用户显式回正触发（兜底，用户随时可用）
  → 用户在输入框里打 "回到需求梳理"
  → 后端意图识别命中回正关键词
  → 当前维度回退到"待追问"状态
  → 重新组装 Prompt（不含刚才偏掉的内容），继续追问
```

```
用户说了一句话
    │
    ▼
后端更新 Session 状态：
  ├── 是否有效维度回答？→ 是 → 标记"已填" → 推进到下一维度
  ├── 是否跑偏了？     → 是 → 保持当前维度 → 注入回正指令
  └── 没实际内容       → 保留状态 → AI 再追同一维度一次
    │
    ▼
组装新 Prompt（含状态表 + 回正指令 if 偏）
    │
    ▼
发给 LLM → 返回用户
```

---

#### 管理端维度配置

需求采集的维度不是写死在代码里的。固定维度不可删除（切换启用状态），灵活维度可以全配置。

**管理端界面结构：**

```
┌──────────────────────────────────────────────────────────────┐
│  需求采集维度配置                                 [新增维度]  │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌── 固定维度（默认启用，不可删除）──────────────────┐       │
│  │                                                     │       │
│  │  ☑ 行业       枚举单选   排序 1     [编辑] [禁用]    │       │
│  │  ☑ 规模       区间选择   排序 2     [编辑] [禁用]    │       │
│  │  ☑ 核心痛点   多行文本   排序 3     [编辑] [禁用]    │       │
│  │  ☑ 预算范围   枚举选择   排序 4     [编辑] [禁用]    │       │
│  │  ☑ 交付时限   日期选择   排序 5     [编辑] [禁用]    │       │
│  │                                                     │       │
│  └─────────────────────────────────────────────────────┘       │
│                                                               │
│  ┌── 灵活维度（可新增 / 删除 / 禁用）─────────────────┐       │
│  │                                                     │       │
│  │  ☑ 已有系统     文本    排序 6    [编辑] [禁用]      │       │
│  │  ☑ 技术栈偏好   枚举    排序 7    [编辑] [禁用]      │       │
│  │  ☐ 安全合规     文本    排序 8    [编辑] [启用]      │       │
│  │  ☐ ERP模块需求  文本    排序 9    [编辑] [删除]      │       │
│  │                                                     │       │
│  │  [+ 新增灵活维度]                                    │       │
│  └─────────────────────────────────────────────────────┘       │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

**每个维度可配置的字段：**

| 配置项 | 说明 | 示例 |
|--------|------|------|
| 维度名称 | 采集时展示给用户和 AI 的标签 | 行业 |
| 数据类型 | TEXT / ENUM / NUMBER / DATE / TEXTAREA | ENUM |
| 枚举值 | 仅 ENUM 型生效，预设选项列表 | 制造业、金融、政府、医疗、教育、互联网、其他 |
| 排序 | AI 追问的优先级顺序 | 1 |
| 是否必填 | 关闭后允许跳过而不触发追问 | 是 |
| 追问话术 | 该维度的引导提问文案 | "请问贵司主要属于哪个行业？" |
| 类型 | 固定 / 灵活 | 固定 |
| 状态 | 启用 / 禁用（禁用后本轮采集跳过该维度） | 启用 |

**数据库表设计：**

```sql
CREATE TABLE requirement_dimension (
    id           BIGINT AUTO_INCREMENT PRIMARY KEY,
    name         VARCHAR(50)  NOT NULL COMMENT '维度名称，如「行业」',
    data_type    VARCHAR(20)  NOT NULL COMMENT 'TEXT/ENUM/NUMBER/DATE/TEXTAREA',
    enum_values  JSON         COMMENT '枚举值列表，如["制造业","金融"]',
    sort_order   INT          NOT NULL DEFAULT 0 COMMENT '追问顺序，越小越优先',
    is_required  TINYINT      NOT NULL DEFAULT 1 COMMENT '0=可选 1=必填',
    prompt_hint  VARCHAR(500) COMMENT '追问话术模板，为空则使用默认',
    is_fixed     TINYINT      NOT NULL DEFAULT 0 COMMENT '0=灵活维度 1=固定维度',
    status       TINYINT      NOT NULL DEFAULT 1 COMMENT '0=禁用 1=启用',
    created_by   VARCHAR(50),
    created_at   DATETIME     NOT NULL,
    updated_at   DATETIME     NOT NULL
) COMMENT '需求采集维度配置表';
```

> **设计要点：** `is_fixed = 1` 的记录不可删除（只能禁用），作为需求采集的最小骨架。`is_fixed = 0` 的记录全量可配置。新增、修改、排序调整均即时生效（清除 Redis 缓存），不需要发版。

---

#### 需求结构化 JSON Schema（初始版）

```json
{
  "固定字段": {
    "客户名称": "string",
    "联系人": "string",
    "行业": "enum(制造业/金融/政府/医疗/教育/互联网/其他)",
    "预算范围": "enum(50万以下/50-200万/200-500万/500万以上)",
    "预期交付时间": "date",
    "核心痛点": "string (多行)"
  },
  "灵活字段（可配置）": {
    "业务场景": "string",
    "技术栈偏好": "string",
    "合规要求": "string",
    "已有系统": "string"
  },
  "AI 衍生": {
    "需求摘要": "string (AI生成)",
    "匹配案例列表": ["case_id", "..."],
    "建议方案类型": "string"
  }
}
```

> **注意：** 灵活字段完全由上述管理端维度配置表驱动，JSON 中的字段名和数据类型与数据库记录一一对应。Demo 阶段先硬编码固定字段，灵活字段的配置化在批次 2 上线。

#### 可行性评估

| 维度 | 评估 | 说明 |
|------|------|------|
| 技术可行性 | ✅ 高 (9/10) | 纯对话 + 结构化提取，无外部系统依赖 |
| 实施难度 | 低 | 核心在 Prompt 设计 + 维度状态机，非复杂工程 |
| 关键依赖 | LLM API + Session 状态管理 + 维度配置表 | |
| Demo 周期 | 约 7 个工作日 | 状态机 2 天 + Prompt 调试 3 天 + 结构化输出 2 天 |

#### 关键风险

| 风险 | 级别 | 缓解措施 |
|------|------|---------|
| LLM 追问偏移（非必要追问） | 🟡 中 | 三层回正机制（Prompt 护栏 + 意图嗅探 + 用户触发） |
| 用户输入不规范难以结构化 | 🟡 中 | 降级为对话摘要文本，人工稍后补填结构化字段 |

---

### 3.3 方案生成 (M3)

#### 功能描述

根据客户需求，自动化生成定制化售前解决方案（PPT / Word）。

#### 模板生命周期

方案生成和标书撰写都依赖模板。模板的管理走一个完整生命周期，不是一次性的：

```
┌────────────────────────────────────────────────────────────────┐
│                      模板生命周期                              │
├────────────────────────────────────────────────────────────────┤
│                                                                 │
│  内置模板（项目自带）           用户导入模板                    │
│  ┌───────────────────┐        ┌───────────────────┐           │
│  │ PPT Template.pptx  │        │ 用户上传的自定义   │           │
│  │ 商务标模板.docx     │        │ PPT / Word 模板    │           │
│  │ 技术标模板.docx     │        │ 含占位符规范       │           │
│  └─────────┬─────────┘        └─────────┬─────────┘           │
│            │                            │                      │
│            └──────────┬─────────────────┘                      │
│                       ▼                                         │
│            ┌─────────────────────┐                              │
│            │    模板入库 + 解析   │                              │
│            │    · 提取占位符列表  │                              │
│            │    · 生成缩略图      │                              │
│            │    · 标注模板类型    │                              │
│            └─────────┬───────────┘                              │
│                      ▼                                          │
│            ┌─────────────────────┐                              │
│            │   用户使用时选择     │                              │
│            │   "用哪个模板生成"   │                              │
│            └─────────┬───────────┘                              │
│                      ▼                                          │
│            ┌─────────────────────┐                              │
│            │   AI 内容 + 模板填充 │                              │
│            │       ↓             │                              │
│            │   在线编辑 + 预览   │                              │
│            │       ↓             │                              │
│            │   用户确认 → 下载   │                              │
│            └─────────────────────┘                              │
│                                                                 │
└────────────────────────────────────────────────────────────────┘
```

**已有模板资源（项目启动即可使用）：**

| 文件 | 大小 | 用途 |
|------|------|------|
| `1. PPT Template.pptx` | 6.8 MB | 售前方案 PPT，含完整排版设计 |
| `商务标——模板(1).docx` | 55 KB | 商务标书模板 |
| `技术标——模板(1).docx` | 947 KB | 技术标书模板 |

> 这些模板在项目初期作为内置模板直接入库。后期 FA 团队可通过管理端上传新模板替换或新增。

---

#### 工作流程

```
需求结构化数据 (M2 输出 JSON)
    │
    ▼
┌──────────────────────────────────────────────────────────┐
│ 阶段①：方案大纲规划                                       │
│                                                           │
│  输入：需求 JSON → 输出：大纲结构（章节列表 + 每节要点）  │
│                                                           │
│  LLM 根据需求的行业/规模/痛点，动态生成方案大纲：           │
│  ┌────────────────────────────────────────────────┐      │
│  │ 1. 项目背景与需求概述                            │      │
│  │ 2. 需求分析与解读                                │      │
│  │ 3. 总体技术方案设计                              │      │
│  │ 4. 产品选型与配置说明                            │      │
│  │ 5. 项目实施计划                                  │      │
│  │ 6. 服务保障与培训                                │      │
│  └────────────────────────────────────────────────┘      │
│                                                           │
│  用户可在此阶段手动调整大纲（增/删/改章节）               │
└──────────────────────┬───────────────────────────────────┘
                       │
┌──────────────────────▼───────────────────────────────────┐
│ 阶段②：逐节内容生成（AI）                                 │
│                                                           │
│  每节独立一次 LLM 调用，上下文 = 完整需求 JSON + 本节大纲 │
│  ┌────────────────────────────────────────────────┐      │
│  │ 调用 1：生成第 1 节 → 结果写入内存              │      │
│  │ 调用 2：需求 JSON + 第 1 节摘要 + 第 2 节大纲   │      │
│  │ 调用 3：需求 JSON + 前两节摘要 + 第 3 节大纲     │      │
│  │ ...                                             │      │
│  └────────────────────────────────────────────────┘      │
│                                                           │
│  前节摘要 ≈ 前节生成的 200 字概括，不是全文               │
│  目的：让后续章节知道前面说了什么，避免内容重复或矛盾     │
└──────────────────────┬───────────────────────────────────┘
                       │
┌──────────────────────▼───────────────────────────────────┐
│ 阶段③：在线编辑 + 预览                                    │
│                                                           │
│  ┌──────────────────────────────────────────────────┐    │
│  │  浏览器端（Vue 3 + Amis）：                        │    │
│  │                                                    │    │
│  │  ┌──────────┐  ┌──────────────────────────────┐  │    │
│  │  │ 大纲导航  │  │  内容编辑区（富文本编辑器）     │  │    │
│  │  │          │  │                              │  │    │
│  │  │ ·第1节   │  │  AI 生成的各节内容展示在这里   │  │    │
│  │  │ ·第2节   │  │  用户可在线逐节编辑、调整      │  │    │
│  │  │ ·第3节   │  │  支持文本格式化 / 表格 / 图片  │  │    │
│  │  │ ...      │  │                              │  │    │
│  │  └──────────┘  └──────────────────────────────┘  │    │
│  │                                                    │    │
│  │  [生成 PDF 预览]  [确认内容无误，下载最终文件]      │    │
│  └──────────────────────────────────────────────────┘    │
│                                                           │
│  用户编辑完成后：                                         │
│    ① 点击预览 → 后台调用 LibreOffice 生成 PDF           │
│    ② 浏览器内嵌 PDF 预览（不离开页面）                    │
│    ③ 用户确认 → 下载最终 PPTX / DOCX 文件                │
│    ④ 如需修改 → 回到编辑区继续改 → 再预览 → 再下载      │
└──────────────────────┬───────────────────────────────────┘
                       │
┌──────────────────────▼───────────────────────────────────┐
│ 阶段④：模板渲染（后台，最终输出）                        │
│                                                           │
│  · 加载用户选择的模板文件（PPTX / DOCX）                  │
│  · python-docx + lxml — 将各节内容填入 Word 模板         │
│  · 生成最终文件 → 存入对象存储 → 返回下载链接             │
└──────────────────────────────────────────────────────────┘
```

---

#### 在线编辑与确认下载流程

这是方案生成的用户交互闭环，不是生成完直接给文件，而是先让用户审查修改：

```
┌─────────────────────────────────────────────────────────────┐
│  用户侧操作流程                                              │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ① 在方案生成页面，选择一个需求记录（来自 M2 的结构化输出）  │
│                              ↓                               │
│  ② 选择模板（内置或自己上传的）                              │
│                              ↓                               │
│  ③ 点击「生成方案」→ AI 逐节生成内容                         │
│     （每节生成完实时展示，不是全部生成完才显示）              │
│                              ↓                               │
│  ④ 在线编辑                                                │
│     ┌─────────────────────────────────────────┐             │
│     │ · 修改文字内容（富文本编辑）              │             │
│     │ · 调整章节顺序 / 删除 / 新增              │             │
│     │ · 补充截图或数据表格                      │             │
│     │ · 每一处修改都是即时保存（不丢数据）      │             │
│     └─────────────────────────────────────────┘             │
│                              ↓                               │
│  ⑤ 生成 PDF 预览                                           │
│     → 后台 LibreOffice 渲染成 PDF                            │
│     → 浏览器内嵌显示，查看最终排版效果                       │
│                              ↓                               │
│  ⑥ 确认或回退                                              │
│     ├── 满意 → 下载 PPTX/DOCX 文件                          │
│     └── 不满意 → 回到步骤 ④ 继续修改                        │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

**这个流程的核心价值：** 客户不是被 AI 取代的，而是 AI 出初稿，人审核润色。确认过的方案才能落盘下载。

---

#### Python 文档生成技术方案对比

| 方案 | 格式 | API 友好度 | 授权成本 | 推荐场景 |
|------|------|-----------|---------|---------|
| **python-docx** | Word | ✅ 简洁 | 免费 (MIT) | **Word 生成首选** |
| **python-pptx** | PPT | ✅ 简洁 | 免费 (MIT) | **PPT 生成首选** |
| **lxml + OOXML** | 全格式 | ⚠️ 底层 XML | 免费 (BSD) | **模板格式保留（在原文档上操作）** |
| **LibreOffice + UNO** | 全格式 | ⚠️ 需维护进程 | 免费 (LGPL) | **格式预览（PPT/DOC → PDF）** |

#### 推荐实施路径

```
阶段 1（可行性验证）：
  内置模板入库（项目目录已有 3 个模板）
  Word → python-docx + lxml（免费，约 3 天出 Demo）
  在线编辑 → 浏览器端 OnlyOffice Web 编辑器

阶段 2（上线交付）：
  Word → python-docx + lxml（不变）
  PPT → python-pptx（按需引入）
  模板导入 → Amis Admin 管理端支持上传/解析/入库
  格式预览 → LibreOffice（PPTX/DOCX → PDF 内嵌预览）
  确认下载 → 预览通过后生成最终文件 + 下载

阶段 3（预算充足时）：
  在线协同编辑 → 多人同时审核同一方案
```

#### 可行性评估

| 维度 | 评估 | 说明 |
|------|------|------|
| 技术可行性 | ✅ 中 (7/10) | 内容生成（AI）低风险，PPT 渲染有技术工作量 |
| 实施难度 | 中高 | 模板填充需逐模板定制占位符映射，在线编辑+预览链路需联调 |
| 关键依赖 | M2 输出的结构化需求 + 模板文件 + LibreOffice + 富文本编辑器 | |
| Demo 周期 | 约 15 个工作日 | 内容生成 5 天 + 在线编辑 3 天 + PPT 渲染 4 天 + PDF 预览 3 天 |

#### 关键风险

| 风险 | 级别 | 缓解措施 |
|------|------|---------|
| PPT 渲染效果不如预期 | 🟡 中 | 先做在线编辑确认内容，再做 PPT 渲染输出，内容与格式分离 |
| 模板格式兼容性 | 🟡 中 | 上传时自动校验占位符规范，不合规的模板提示用户修正 |
| 在线编辑内容丢数据 | 🟡 中 | 每次编辑自动保存草稿（Redis/MySQL 双写），不依赖浏览器本地存储 |

---

### 3.4 标书撰写 (M4)

#### 功能描述

标书智能撰稿、格式校验、历史标书素材复用。

标书和方案生成（M3）共享同一套模板系统、python-docx 渲染、在线编辑预览下载流程。但标书有自己独特的三个难题——也是本章要讲清楚的核心：**招标需求解析、逐条响应不漏项、素材复用 + 格式校验。**

---

#### 一、招标需求解析

这是标书撰写的第一步，也是最容易翻车的一步。招标文件格式没有任何标准——有的把需求列在表格里，有的分散在正文段落中，有的混在一起。

**解析策略 — 两轮 LLM 提取，不要一轮试图全搞定：**

```
招标文件上传 (PDF/Word)
    │
    ▼
┌─────────────────────────────────────────────────────────┐
│ 第一轮：全文提取（粗筛）                                  │
│                                                          │
│  输入：招标文件全文（超长则分段 + 汇总）                  │
│  LLM 任务：找出文档中所有"投标人必须满足"的条款          │
│  输出：需求清单 JSON                                     │
│  [                                                       │
│    {"id":"REQ-001","原文":"投标人须具备CMMI3级以上资质",  │
│                        "所在位置":"第三章 2.1节"},         │
│    {"id":"REQ-002","原文":"项目团队至少配备2名PMP认证...",│
│                        "所在位置":"第三章 2.3节"},         │
│    ...                                                   │
│  ]                                                       │
│                                                          │
│  关键设计：每条需求带"所在位置"，用户可回到原文核对        │
└──────────────────────┬──────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────┐
│ 第二轮：逐条分类（精筛）                                  │
│                                                          │
│  输入：第一轮输出的每条需求                               │
│  LLM 任务：判断每条属于哪一类                             │
│                                                          │
│  分类体系：                                              │
│  ┌──────────────┬──────────────────────────────────┐    │
│  │ 硬性条款      │ 不满足就废标，必须逐条响应          │    │
│  │ 加分项       │ 满足加分，不满足不扣分              │    │
│  │ 格式要求      │ 页数限制/字体/密封方式/盖章要求     │    │
│  │ 证明材料      │ 需要附上的资质证书/合同复印件       │    │
│  │ 商务条款      │ 付款方式/质保期/售后服务要求        │    │
│  └──────────────┴──────────────────────────────────┘    │
│                                                          │
│  输出：带分类标签的需求清单                               │
│  [                                                       │
│    {"id":"REQ-001","原文":"...","分类":"硬性条款",        │
│     "需响应内容":"提供CMMI3级证书复印件"},                 │
│    ...                                                   │
│  ]                                                       │
└──────────────────────┬──────────────────────────────────┘
                       │
┌──────────────────────▼──────────────────────────────────┐
│ 人工核验（必须在 AI 之后）                                │
│                                                          │
│  界面展示：原文（高亮位置）↔ AI 提取结果（并排对比）     │
│  用户可以：                                              │
│    · 确认提取结果正确                                     │
│    · 修正分类错误（加分项被标成了硬性条款）               │
│    · 补充 AI 漏掉的需求                                   │
│    · 删除 AI 误提取的内容                                 │
│                                                          │
│  确认后 → 需求清单锁定，作为后续撰写的基准                │
└──────────────────────────────────────────────────────────┘
```

**为什么要两轮而不是一轮？** 一轮 LLM 同时做提取 + 分类，边界案例容易出错。两轮把"找到"和"分类"分开，每轮只做一件事，准确率更高。而且第一轮输出可以人工核验后再进第二轮，降低错误传播。

---

#### 二、逐条响应机制

标书不是自由写作。硬性条款漏了一条就可能废标。核心设计是 **需求-响应映射表**，确保每条需求都有对应、可追溯。

```
┌─────────────────────────────────────────────────────────────┐
│               逐条响应追踪系统                                │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  需求清单（从招标解析阶段锁定）                               │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ REQ-001 │ 硬性条款 │ CMMI3资质        │ ✅ 已响应     │   │
│  │ REQ-002 │ 硬性条款 │ 2名PMP           │ ✅ 已响应     │   │
│  │ REQ-003 │ 加分项   │ 同类项目经验3个   │ ⚠️ 待补充    │   │
│  │ REQ-004 │ 商务条款 │ 3年质保承诺       │ ⏳ AI生成中   │   │
│  │ REQ-005 │ 格式要求 │ 不超过200页       │ ⏳ 人工填写   │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  响应状态流转：                                              │
│    ⏳ 待处理 → 🤖 AI 生成中 → 📝 待人工审核 → ✅ 已完成      │
│                                                              │
│  每条响应的数据结构：                                        │
│  {                                                           │
│    "requirement_id": "REQ-001",                              │
│    "category": "硬性条款",                                   │
│    "original_text": "投标人须具备CMMI3级以上资质",           │
│    "response_content": "我司于2023年通过CMMI5级认证...",     │
│    "evidence_file": "cmmi5_cert.pdf",                        │
│    "status": "已完成",                                       │
│    "reviewed_by": "张工",                                    │
│    "source_material": ["历史标书#42-资质章节"]  // 如有复用  │
│  }                                                           │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

**技术标 vs 商务标的分流生成：**

```
需求清单确认后
    │
    ├── 技术标路径（技术方案类需求）
    │   ├── REQ: 系统需支持信创环境
    │   │   → AI 生成技术响应内容
    │   │   → 引用产品文档 + 历史标书中的信创方案段落
    │   │
    │   ├── REQ: 项目团队需配备PMP认证项目经理
    │   │   → AI 生成团队介绍（从公司人员库拉数据）
    │   │
    │   └── ...所有技术类需求逐条响应完
    │       → python-docx 填入技术标模板 → 技术标.docx
    │
    ├── 商务标路径（资质/报价/商务条款类需求）
    │   ├── REQ: CMMI3级以上资质
    │   │   → AI 生成资质说明 + 引用证书文件
    │   │
    │   ├── REQ: 同类项目经验
    │   │   → 从历史项目库拉匹配的合同/验收单
    │   │
    │   └── ...所有商务类需求逐条响应完
    │       → python-docx 填入商务标模板 → 商务标.docx
    │
    └── 合并检查
        ├── 是否有硬性条款未响应？→ 有就标红，不允许导出
        ├── 所有证明材料是否已附加？
        └── 格式校验通过？→ 合并下载
```

**防漏项机制 — 三道检查：**

| 检查点 | 时机 | 规则 |
|--------|------|------|
| 需求映射检查 | AI 生成完成后 | 每条硬性条款必须有响应内容，否则标红阻塞 |
| 人工核验 | 生成完成后 | 用户逐条确认，勾选"已审核" |
| 导出前最终检查 | 点击下载时 | 后端扫描：硬性条款响应率必须 100%，否则拒绝导出 |

---

#### 三、历史标书素材复用

和技术问答（M1）共享 Milvus 基础设施，但做的是"改写适配"而不是"检索回答"。

```
┌─────────────────────────────────────────────────────────────┐
│              历史标书素材复用全流程                          │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  入库阶段（一次性，后续增量维护）                             │
│  ───────────────────────────────                             │
│  历史中标标书 (.docx / .pdf)                                 │
│      │                                                       │
│      ▼                                                       │
│  ┌──────────────────────────────────────────────┐           │
│  │ 文档拆分引擎                                  │           │
│  │ · 按章节/大标题自动切分                       │           │
│  │ · 最小粒度：一个自然段落                      │           │
│  │ · 保留元数据：所属章节/文档来源/中标时间       │           │
│  └──────────────────────┬───────────────────────┘           │
│                         ▼                                    │
│  ┌──────────────────────────────────────────────┐           │
│  │ Embedding 向量化 → 存入 Milvus               │           │
│  │ 附带标量字段（用于过滤）：                     │           │
│  │   · project_type:  "ERP"/"大数据"/"信创"...   │           │
│  │   · industry:      "政府"/"金融"/"制造"...    │           │
│  │   · bid_amount:    2000000                   │           │
│  │   · result:        "中标"/"未中标"            │           │
│  │   · date:          "2024-06"                 │           │
│  └──────────────────────────────────────────────┘           │
│                                                              │
│  使用阶段（标书撰写时触发）                                   │
│  ─────────────────────────                                   │
│  当前正在写 REQ-001 的响应（CMMI 资质相关内容）              │
│      │                                                       │
│      ▼                                                       │
│  ┌──────────────────────────────────────────────┐           │
│  │ 混合检索（Milvus 向量 + 标量过滤）            │           │
│  │                                              │           │
│  │  向量检索：语义相似（"资质 CMMI 认证"）       │           │
│  │  标量过滤：result="中标"                      │           │
│  │           project_type≈当前项目类型           │           │
│  │                                              │           │
│  │  → 返回 Top-3 匹配段落                       │           │
│  └──────────────────────┬───────────────────────┘           │
│                         ▼                                    │
│  ┌──────────────────────────────────────────────┐           │
│  │ AI 改写适配（不是复制粘贴！）                 │           │
│  │                                              │           │
│  │  Prompt 包含：                                │           │
│  │   ① 历史中标段落原文（仅供参考）              │           │
│  │   ② 当前项目的实际信息（公司名/证书级别/日期） │           │
│  │   ③ 改写约束：保留行文风格 + 替换为新项目信息  │           │
│  │                                              │           │
│  │  → 输出：适配当前项目的新段落                 │           │
│  └──────────────────────┬───────────────────────┘           │
│                         ▼                                    │
│  ┌──────────────────────────────────────────────┐           │
│  │ 用户审核                                       │           │
│  │                                              │           │
│  │  界面并排展示：                                │           │
│  │   左：历史原文（标注来源标书 + 中标情况）      │           │
│  │   右：AI 改写后内容（高亮替换了哪些信息）       │           │
│  │                                              │           │
│  │  用户操作：                                    │           │
│  │   [采纳改写] [使用原文不改] [放弃不用自己写]    │           │
│  └──────────────────────────────────────────────┘           │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

**素材复用和 M1 技术问答的关系：**

| 维度 | M1 技术问答 | M4 素材复用 |
|------|------------|-----------|
| 目的 | 回答用户问题 | 提供可改写的历史段落 |
| 检索结果用途 | LLM 阅读后生成回答 | 用户看到原文对照改写结果 |
| 数据源 | 产品文档 + FA 案例 + 政策库 | 历史中标标书库（单独集合） |
| 后续操作 | 无（回答即终点） | 审核 → 改写 → 插入当前标书 |
| Milvus Collection | `product_docs` / `case_library` / `policy_library` | `bid_material_library`（独立集合） |

---

#### 四、格式校验

分两层——文档格式检查和内容完整性检查。

**第一层：文档格式检查 — 后端自动执行**

| 检查项 | 检查方式 | 示例规则 |
|--------|---------|---------|
| 字体 | POI 读取文档字体属性 | 正文宋体小四、标题黑体三号 |
| 页边距 | POI 读取页面设置 | 上下 2.54cm，左右 3.17cm |
| 页码 | 检查页脚是否含页码 | 必须在页脚居中 |
| 页数限制 | 统计总页数 | ≤200 页（来自招标要求） |
| 目录完整性 | 对比大纲与实际内容 | 目录 8 章，正文 8 章，对齐 |
| 签字盖章位置 | 检查指定位置是否有图片/占位符 | 末页必须含公章扫描位 |

校验规则不是写死在代码里的。规则存在数据库中，每次校验从数据库拉当前生效规则集：

```sql
CREATE TABLE bid_format_rule (
    id            BIGINT AUTO_INCREMENT PRIMARY KEY,
    rule_name     VARCHAR(100) NOT NULL COMMENT '规则名称',
    check_target  VARCHAR(50)  NOT NULL COMMENT '检查对象: FONT/MARGIN/PAGE_NUM/TOC/SEAL',
    check_method  VARCHAR(50)  NOT NULL COMMENT '检查方式: PROPERTY_READ/RANGE_CHECK/EXISTENCE_CHECK',
    rule_config   JSON         NOT NULL COMMENT '规则参数，如 {"font":"宋体","size":12}',
    severity      VARCHAR(20)  NOT NULL DEFAULT 'WARN' COMMENT 'ERROR=阻塞导出 WARN=提示 INFO=建议',
    status        TINYINT      NOT NULL DEFAULT 1,
    created_at    DATETIME     NOT NULL,
    updated_at    DATETIME     NOT NULL
) COMMENT '标书格式校验规则表';
```

**第二层：内容完整性检查 — AI + 后端联合**

| 检查项 | 方式 | 说明 |
|--------|------|------|
| 硬性条款响应率 | 后端统计 | 需求-响应映射表中硬性条款 status != "已完成" 的条数，> 0 则阻塞导出 |
| 证明材料完整性 | 后端校验 | REQ 需附证书 → 检查 evidence_file 字段是否为空 |
| 章节编号连续性 | 后端检查 | 1.1 → 1.2 → 1.3 中间不能跳号 |
| 前后矛盾检测 | AI 检查 | "项目周期 6 个月" vs "实施计划 12 个月" → AI 标记矛盾 |
| 报价一致性 | 后端检查 | 商务标中的总价 = 分项报价之和 |

**导出前检查流水线：**

```
用户点击 [导出标书]
    │
    ▼
┌─────────────────────┐
│ 第一层：格式检查     │   后端自动，秒级完成
│ · 字体/页边距/页码  │
│ · 页数是否符合限制   │
│   ↓                  │
│  ERROR 类不合格 → 阻塞，展示问题清单
│  WARN 类不合格 → 提示，用户可选择忽略
└─────────┬───────────┘
          │ 通过（或无阻塞性问题）
          ▼
┌─────────────────────┐
│ 第二层：内容完整性   │   后端统计 + AI 检查
│ · 硬性条款 100% 响应 │
│ · 证明材料齐全       │
│ · 章节编号连续       │
│ · 前后无矛盾          │
│   ↓                  │
│  任一不通过 → 阻塞，展示具体问题
└─────────┬───────────┘
          │ 全部通过
          ▼
┌─────────────────────┐
│ 生成最终文件 + 下载  │
└─────────────────────┘
```

---

#### 可行性评估

| 维度 | 评估 | 说明 |
|------|------|------|
| 技术可行性 | ✅ 中 (7/10) | 内容生成可行，招标需求解析准确性需要实际文件验证 |
| 实施难度 | 中高 | 逐条响应追踪系统 + 两轮解析 LLM Pipeline + 格式校验规则引擎 |
| 关键依赖 | 历史标书素材库 + 招标文件解析能力 + 需求-响应映射表 + 格式规则配置 |
| Demo 周期 | 约 20 个工作日 | 需求解析 7 天 + 逐条响应引擎 5 天 + 素材复用 4 天 + 格式校验 4 天 |

#### 现有可复用资源

项目目录已有客户提供的模板：
- `商务标——模板(1).docx`（55 KB）
- `技术标——模板(1).docx`（947 KB）

可直接作为内置模板入库。在线编辑、PDF 预览、确认下载流程完全复用 M3 方案生成的同一套机制。

#### 关键风险

| 风险 | 级别 | 缓解措施 |
|------|------|---------|
| PDF 招标文件解析质量不稳定（表格/扫描件） | 🔴 高 | 限定支持 PDF + Word 格式；扫描件 PDF 不支持（需客户提供可复制文本版本），两轮 LLM 提取 + 人工核验兜底 |
| 历史素材库初期为空（没有历史中标标书可参考） | 🟡 中 | 先靠 AI 从零生成（质量取决于 Prompt + 需求数据），素材库随使用逐步积累；至少导入 5-10 份历史标书做种子 |
| 标书格式规范严格（字体/页边距/页码等） | 🟡 中 | 格式校验规则可配置（数据库驱动），从客户已有的模板中逆向提取格式规则作为初始配置 |
| 硬性条款漏响应（直接导致废标） | 🔴 高 | 需求-响应映射表 + 导出前三道检查；硬性条款响应率 ≠ 100% 时阻塞导出并标红 |

---

### 3.5 政策打通 (M5)

#### 功能描述

接入全国各省市社保、公积金等属地政策知识库，为标书撰写和技术问答提供政策数据支撑。

---

#### 一、两路采集 + 统一入库

政策数据有两条来源路径。关键在于两条路径最终汇入同一张表、同一个 Schema，后续才能一起检索和引用。

```
┌──────────────────────────────────────────────────────────────────┐
│               两路数据采集 → 统一入库                             │
├──────────────────────────────────────────────────────────────────┤
│                                                                   │
│  AI 自动抓取路径                      手动录入路径               │
│  ──────────────                      ──────────                  │
│                                                                   │
│  ① APScheduler 定时任务                ① FA 团队在管理端           │
│     每周扫描目标 URL 列表                打开结构化录入表单        │
│        │                                  │                      │
│        ▼                                  ▼                      │
│  ② 页面 HTML 抓取                     ② 按统一 Schema 填写       │
│     → LLM 从 HTML 中提取结构化字段         · 省/市 下拉选择        │
│        · 省/市/类别/生效日期/全文          · 类别 下拉选择         │
│        · 来源 URL 自动记录                 · 生效/失效日期         │
│        │                                  · 全文 富文本粘贴        │
│        ▼                                  │                      │
│  ③ 生成结构化 JSON                       ▼                      │
│     （与手动录入完全相同的字段结构）     ③ 直接生成结构化 JSON    │
│        │                                  │                      │
│        └────────────┬─────────────────────┘                      │
│                     │                                             │
│                     ▼                                             │
│  ┌──────────────────────────────────────────┐                    │
│  │  统一入库 — policy_record 表（同一张表）  │                    │
│  │  ┌───────────────────────────────────┐  │                    │
│  │  │ province:      "广东省"            │  │                    │
│  │  │ city:          "广州市"            │  │                    │
│  │  │ category:      "社保"              │  │                    │
│  │  │ effective_date:"2026-01-01"        │  │                    │
│  │  │ expire_date:   "2026-12-31"        │  │                    │
│  │  │ content:       "..."              │  │                    │
│  │  │ source_url:    "http://..."       │  │                    │
│  │  │ source_type:   "AI_SCRAPE"/"MANUAL"│ ← 区分来源             │
│  │  │ status:        "PENDING"           │ ← 统一进审核队列        │
│  │  └───────────────────────────────────┘  │                    │
│  └──────────────────────┬───────────────────┘                    │
│                         │                                         │
│                         ▼                                         │
│              进入人工审核流程（见第二节）                          │
│                                                                   │
└──────────────────────────────────────────────────────────────────┘
```

**格式统一的核心原则：**

| 原则 | 说明 |
|------|------|
| **同一张表，同一个 Schema** | AI 抓的和人填的，落库后字段完全一样。唯一区别是 `source_type` 标记来源 |
| **AI 负责格式规范化** | AI 抓下来的 HTML 格式千差万别，但 AI 提取后的结构化 JSON 字段是统一格式 |
| **统一进审核队列** | AI 抓取不是直接发布，和人填的一样走审核流程 |
| **手动录入可快捷发布** | 管理员手动录入的可选跳过审核直接发布；AI 抓取的不允许 |

---

#### 二、人工审核流程 — 全生命周期

```
┌──────────────────────────────────────────────────────────────────────┐
│                    政策数据全生命周期                                  │
├──────────────────────────────────────────────────────────────────────┤
│                                                                       │
│                        AI 抓取 / 手动录入                              │
│                              │                                        │
│                              ▼                                        │
│  ┌─────────────────────────────────────────────────────────┐         │
│  │              状态：待审核 (PENDING)                       │         │
│  │                                                          │         │
│  │  审核界面（并排对比布局）：                                │         │
│  │  ┌─────────────────┐  ┌───────────────────────────┐     │         │
│  │  │ 原始来源参考     │  │ 结构化数据（可逐字段编辑）  │     │         │
│  │  │                  │  │                           │     │         │
│  │  │ AI抓取：显示原文 │  │ 省：广东省         ✓      │     │         │
│  │  │ 快照（URL可点击) │  │ 市：广州市         ✓      │     │         │
│  │  │                  │  │ 类别：社保         ✓      │     │         │
│  │  │ 手动录入：显示   │  │ 生效日期：2026-01-01      │     │         │
│  │  │ 录入人 + 时间    │  │ 失效日期：2026-12-31      │     │         │
│  │  │                  │  │ 全文：...                 │     │         │
│  │  └─────────────────┘  └───────────────────────────┘     │         │
│  │                                                          │         │
│  │  审核人操作：                                             │         │
│  │  ┌─────────────────────────────────────────────────┐    │         │
│  │  │ [✓ 通过发布] [✏️ 编辑后发布] [✗ 驳回（填写原因）]  │    │         │
│  │  └─────────────────────────────────────────────────┘    │         │
│  └──────────────────────────┬──────────────────────────────┘         │
│                             │                                         │
│               ┌─────────────┼─────────────┐                           │
│               ▼             ▼             ▼                           │
│            通过         编辑后通过       驳回                          │
│               │             │             │                           │
│               ▼             ▼             ▼                           │
│  ┌────────────────┐ ┌──────────────┐ ┌──────────────┐                │
│  │status=         │ │status=       │ │status=        │               │
│  │  PUBLISHED     │ │  PUBLISHED   │ │  DRAFT        │               │
│  │                │ │+ 修改审计记录 │ │+ 驳回原因      │               │
│  └───────┬────────┘ └──────────────┘ │+ 通知录入人    │               │
│          │                           └──────────────┘                │
│          ▼                                                            │
│  ┌─────────────────────────────────────────────────────────┐         │
│  │              状态：已发布 (PUBLISHED)                     │         │
│  │                                                          │         │
│  │  · 写入 MySQL（结构化字段可精确查询）                     │         │
│  │  · 写入 Milvus（全文 Embedding 可语义检索）               │         │
│  │  · M1 技术问答可检索引用                                  │         │
│  │  · M4 标书撰写可引用（自动检查有效期）                    │         │
│  │  · 来源 URL 可追溯                                        │         │
│  └──────────────────────────┬──────────────────────────────┘         │
│                             │                                         │
│                             ▼                                         │
│  ┌─────────────────────────────────────────────────────────┐         │
│  │         定时巡检（APScheduler，每周执行）                  │         │
│  │                                                          │         │
│  │  巡检内容（自动执行 + 不自动改状态，只产生待办提醒）：     │         │
│  │                                                          │         │
│  │  ① 过期检测：now > expire_date                            │         │
│  │     → 自动标记 EXPIRED（唯一自动状态变更）                │         │
│  │                                                          │         │
│  │  ② 源链验证：source_url 仍可访问？                        │         │
│  │     → 404 → 待办："来源链接失效，请更新"                  │         │
│  │                                                          │         │
│  │  ③ 新政策关联：同一省/市/类别有更近的 published 记录？    │         │
│  │     → 有 → 待办："可能存在更新政策，旧记录是否需要过期？" │         │
│  │                                                          │         │
│  │  ④ 重复冲突：同一省/市/类别有多条 PUBLISHED？             │         │
│  │     → 有 → 待办："存在重复记录，建议合并或标注冲突"       │         │
│  └──────────────────────────┬──────────────────────────────┘         │
│                             │                                         │
│                             ▼                                         │
│  ┌─────────────────────────────────────────────────────────┐         │
│  │              状态：已过期 (EXPIRED)                       │         │
│  │                                                          │         │
│  │  · 不再出现在 M1/M4 的默认搜索结果中                     │         │
│  │  · M4 标书若已引用 → 标黄提示"该政策已过期"              │         │
│  │  · 保留在数据库用于历史追溯（不删除）                     │         │
│  │  · 可手动恢复为 PUBLISHED（如判断过期有误）               │         │
│  └──────────────────────────────────────────────────────────┘         │
│                                                                       │
└──────────────────────────────────────────────────────────────────────┘
```

**几条关键设计约束：**

| 约束 | 为什么 |
|------|--------|
| AI 抓取不直接发布 | AI 不知道政策是否已被内部讨论废止，必须人看一眼 |
| 新旧政策关联不自动 | 新政策出现 ≠ 旧政策一定作废（可能是补充而非替代），人工判断 |
| 复审不删除记录 | 打回 DRAFT 而非 DELETE，录入人修正后重新提交 |
| 巡检只提醒不改状态 | expire_date 到期是唯一自动状态变更，其余只产生待办 |
| 驳回必有原因 | 审核人不能无理由驳回，录入人需要知道怎么改 |

---

#### 三、政策过时管理

这是 M5 最高风险项。标书引用了过时政策可能导致废标。

**三种过时场景及对策：**

```
场景一：政策有明确截止日期
─────────────────────────────
  例：某市社保补贴政策 2025-01-01 生效，2025-12-31 截止
  
  → 人工录入时填写 expire_date = 2025-12-31
  → 每周巡检：now > 2025-12-31 → 自动标记 EXPIRED
  → M4 引用时自动检查：当前时间 > expire_date → 标黄警告

场景二：旧政策被新政策替代（无明确截止日期）
─────────────────────────────────────────────
  例：2024 版公积金缴存比例 → 2026 版新标准发布
  
  → 录入新政策时：人工把旧政策的 expire_date 改为新政策的
    生效日期 2026-01-01
  → 不是自动替，因为需要人工判断"新政策是真的替代还是只是补充件"

场景三：市级规定覆盖省级
─────────────────────────────
  例：广东省统一比例 5%，深圳市单独规定 6%
  
  → 查询时优先级：市 > 省 > 全国
  → M1/M4 检索时：输入"深圳"→ 先搜深圳政策，无结果再降级到广东，再不中降级到全国
  → 不存在"深圳把广东的过期掉"——两者都有效，查询优先级决定命中哪个
```

**省市区三级级联查询规则：**

```
政策查询优先级（后端检索层实现）：

  输入：目标省 + 目标市
    │
    ├── ① 精确匹配（省=广东，市=深圳）→ 找到 → 返回
    │
    ├── ② 省级降级（省=广东，市=NULL）→ 找到 → 返回
    │
    └── ③ 全国兜底（省=NULL，市=NULL）→ 找到 → 返回
                                          ↓
                                      都没找到 → 返回"暂无相关政策"
```

---

#### 可行性评估

| 维度 | 评估 | 说明 |
|------|------|------|
| 技术可行性 | ✅ 中高 (8/10) | 技术简单（结构化+向量混合存储 + 定时巡检），不靠 AI 做复杂决策 |
| 实施难度 | 低 | 大部分是 CRUD + 状态机 + 定时任务，标准 FastAPI 开发模式 |
| 关键依赖 | 政策数据来源 URL 清单 + 审核人员配置 + 初始覆盖省份 | |
| Demo 周期 | 约 10 个工作日 | 两路采集 4 天 + 审核工作流 3 天 + 巡检任务 2 天 + Milvus 同步 1 天 |

#### 政策数据覆盖建议

| 阶段 | 覆盖范围 | 说明 |
|------|---------|------|
| 启动期 | 3-5 个重点省份 | 根据客户当前业务量最大的省市 |
| 扩展期 | 10-15 个省份 | 覆盖业务增长区域 |
| 完整期 | 31 省 + 重点城市 | 市级差异化政策持续维护 |

#### 关键风险

| 风险 | 级别 | 缓解措施 |
|------|------|---------|
| 政策变动导致知识库过时 | 🔴 高 | 生效/失效日期窗口 + 每周定时巡检 + 新旧关联待办提示；过期政策标书引用时标黄 |
| AI 抓取准确率不足（字段提取错误） | 🟡 中 | AI 抓取 = 初稿，100% 走人工审核后才发布；审核界面并排对比原文 |
| 各省市格式差异导致入库数据不一致 | 🟡 中 | AI 抓取阶段即规范化为统一 Schema；人工录入走标准化表单（下拉枚举 + 日期选择器） |
| 两路数据冲突（同一政策被 AI 抓了又被人手动录了） | 🟡 中 | 入库前去重检查（省+市+类别+生效日期四字段组合去重），发现重复 → 合并提示 |

---

## 4. 技术选型汇总

| 维度 | 决策结果 | 替代方案 | 备注 |
|------|---------|---------|------|
| 后端框架 | **FastAPI (Python)** | Django / Spring Boot | 异步高性能，AI 生态原生 |
| 管理后台 | **Amis Admin（低代码）** | 纯 Vue 3 | JSON 配置化，零前端代码 CRUD |
| 用户交互前端 | **Vue 3 + Element Plus** | React | 对话/编辑器等复杂交互页面 |
| ORM | **SQLModel + SQLAlchemy** | Tortoise ORM | Pydantic 融合，同步/异步双模式 |
| 大模型 | **第三方 API（DeepSeek/Qwen）** | 私有化部署 | 建议初期用 API 快速验证 |
| 向量数据库 | **Milvus（独立实例，pymilvus）** | Pgvector / ES | Python SDK 原生支持 |
| 业务数据库 | **MySQL / PostgreSQL** | — | SQLModel 多数据库适配 |
| Word 生成 | **python-docx + lxml** | Aspose.Words | Python 原生，格式保留强 |
| 格式预览 | **LibreOffice + JODConverter** | 在线 Office | 免费，PPT/DOC → PDF |
| 模板管理 | **Amis Admin + 自研上传** | — | 管理端零代码，用户端自定义 |
| 政策数据采集 | **AI 抓取 + 人工审核** | 纯人工/第三方购买 | 双轨制保证质量 |
| AI 检索方式 | **RAG（向量检索）+ NL2SQL（结构化查询）** | 纯 RAG | 覆盖非结构化和结构化数据 |

---

## 5. 分批迭代计划

### 5.1 总体路线

```
批次 1 — 基础设施（Q3 启动）
  ├── M1  技术问答（核心 RAG + NL2SQL 链路）
  ├── M5  政策打通（数据采集 + 知识库搭建）
  ├──     Milvus 向量库搭建
  └──     模板管理系统搭建
       │
批次 2 — 上层应用（Q3 末 / Q4 初）
  ├── M2  需求梳理（引导式对话 + 结构化输出）
  ├── M3  方案生成（AI 内容生成 + POI 渲染）
  └── M4  标书撰写（智能撰稿 + 素材复用）
```

### 5.2 分批理由

```
模块依赖关系图：

    M5 (政策打通)
     │
     ▼
M1 (技术问答) ─→ M2 (需求梳理) ─→ M3 (方案生成)
     │                              │
     └────→ M4 (标书撰写) ←─────────┘

核心逻辑：
- M1 和 M5 是整个系统的数据基础（知识库 + 政策库）
- M2、M3、M4 依赖 M1 的基础能力（LLM 编排 + 检索框架）
- 基础设施先行，上层应用复用

批次 1 = 铺管道（数据底座 + AI 基座）
批次 2 = 接龙头（上层业务应用）
```

### 5.3 里程碑

| 里程碑 | 时间 | 交付物 |
|--------|------|--------|
| K0: 可行性确认 | 当前 | 本报告 |
| K1: Milvus + LLM 链路打通 | 批次 1 第 1 周 | RAG Demo，可问答 FA 案例 |
| K2: 模板管理上线 | 批次 1 第 2 周 | 模板上传/选择/填充能力 |
| K3: 技术问答 MVP | 批次 1 第 3 周 | 可回答问题 + 可查询数据 |
| K4: 政策知识库启动 | 批次 1 第 4 周 | 首批 3-5 省政策数据入库 |
| K5: 需求梳理上线 | 批次 2 第 1 周 | 引导式对话 + 结构化输出 |
| K6: 方案生成上线 | 批次 2 第 3 周 | AI 方案 + PPT 输出 |
| K7: 标书撰写上线 | 批次 2 第 4 周 | 标书生成 + 素材复用 |
| K8: 第一阶段 GA | Q4 | 全模块可用 |

---

## 6. 风险矩阵

### 6.1 全局风险汇总

| # | 风险 | 影响模块 | 概率 | 影响 | 级别 | 缓解策略 |
|---|------|---------|------|------|------|---------|
| R1 | 知识库初始无数据 | M1 | 🔴 高 | 🔴 高 | **P0** | 要求客户提前准备 20+ 案例文档 + 产品技术文档 |
| R2 | 硬性条款漏响应 → 废标 | M4 | 🟡 中 | 🔴 高 | **P0** | 需求-响应映射表强制核对，导出前硬性条款响应率 ≠ 100% 阻塞下载 |
| R3 | 政策变动导致知识库过时 | M5 | 🔴 高 | 🔴 高 | **P0** | 生效/失效日期窗口 + 每周巡检 + 新旧关联待办 + 过期引用标黄 |
| R4 | LLM API 不稳定 | M1-M4 | 🟡 中 | 🔴 高 | **P1** | 多模型路由 + Prompt 降级策略 + 重试机制 |
| R5 | PDF 招标文件解析不准（含表格/扫描件） | M4 | 🟡 中 | 🟡 中 | **P1** | 两轮 LLM 提取（粗筛+精筛）；不支持扫描件 PDF；复杂表格人工核验 |
| R6 | PPT 渲染效果不理想 | M3 | 🟡 中 | 🟡 中 | **P2** | 内容与格式分离——先做在线编辑确认内容，PPT 渲染可迭代优化 |
| R7 | NL2SQL 查询准确率不足 | M1 | 🟡 中 | 🟡 中 | **P2** | 限定预定义查询范围；复杂查询走人工兜底 |
| R8 | LLM 追问偏移（非必要追问/主题漂移） | M2 | 🟡 中 | 🟡 中 | **P2** | 三层回正机制——Prompt 状态表护栏 + 后端意图嗅探 + 用户显式触发回正 |
| R9 | 模板格式兼容性 | M3, M4 | 🟡 中 | 🟡 中 | **P2** | 模板上传时自动校验占位符规范，不合规的提示修正 |
| R10 | 在线编辑内容丢数据 | M3, M4 | 🟡 中 | 🟡 中 | **P2** | 每次编辑自动保存草稿（Redis + MySQL 双写） |
| R11 | AI 抓取政策准确率不足 | M5 | 🟡 中 | 🟡 中 | **P2** | AI 抓取 = 初稿，100% 走人工审核后发布 |
| R12 | AI 抓取与手动录入数据冲突 | M5 | 🟡 中 | 🟡 中 | **P2** | 入库前去重（省+市+类别+生效日期四字段组合）；重复 → 合并提示 |
| R13 | 历史标书素材库初期为空 | M4 | 🟡 中 | 🟡 中 | **P2** | 先靠 AI 从零生成；至少导入 5-10 份历史标书作种子素材库 |
| R14 | 用户输入不规范难以结构化 | M2 | 🟡 中 | 🟢 低 | **P3** | 降级为对话摘要文本，人工稍后补填结构化字段 |

### 6.2 风险等级定义

| 等级 | 定义 | 应对策略 |
|------|------|---------|
| **P0** | 阻塞性 — 不解决，项目不可上线 | 必须在开发启动前制定经客户确认的缓解方案 |
| **P1** | 严重 — 上线前必须缓解 | 开发阶段优先投入，上线前验收 |
| **P2** | 一般 — 开发过程中可控 | 按模块开发节奏逐步处理，MVP 阶段可降级为 WARN |
| **P3** | 低 — 常规监控即可 | 仅记录，不影响发版节奏 |

### 6.3 按模块风险分布

| 模块 | P0 | P1 | P2 | P3 | 风险集中度 |
|------|----|----|----|----|-----------|
| M1 技术问答 | R1 | R4 | R7 | — | 中 |
| M2 需求梳理 | — | R4 | R8 | R14 | 低 |
| M3 方案生成 | — | R4 | R6, R9, R10 | — | 中 |
| M4 标书撰写 | R2 | R4, R5 | R9, R10, R13 | — | **高** |
| M5 政策打通 | R3 | — | R11, R12 | — | 中高 |

> **解读：** M4 标书撰写是风险最集中的模块（2 个 P0 + 2 个 P1），因为硬性条款漏响应和招标文件解析糟糕的后果都是废标。建议在开发计划中给 M4 留充足的测试和人工校验时间。

---

## 7. 资源估算

### 7.1 预估工作量

| 阶段 | 模块 | 预估人天 |
|------|------|---------|
| **批次 1** | M1 技术问答 | 10 天 |
| | M5 政策打通 | 10 天 |
| | 基础设施（Milvus / 模板管理） | 10 天 |
| **批次 2** | M2 需求梳理 | 7 天 |
| | M3 方案生成 | 15 天 |
| | M4 标书撰写 | 20 天 |
| **合计** | **不含项目管理/沟通** | **约 72 人天** |

> 注：以上为纯开发工作量估算，不包含需求沟通、UI 设计、测试、部署、数据准备。

### 7.2 外部依赖

| 依赖 | 用途 | 备注 |
|------|------|------|
| Milvus 实例 | 向量数据库 | 需部署（可 Docker 或云服务） |
| LLM API 密钥 | AI 能力 | DeepSeek / Qwen 等 |
| LibreOffice | 格式转换（PPT→PDF） | 服务器静默安装 |
| 对象存储（MinIO/S3） | 模板/素材文件存储 | FastAPI 插件生态支持 |
| FA 案例文档 | 知识库初始数据 | 客户需提供 |

### 7.3 团队建议

| 角色 | 人数 | 职责 |
|------|------|------|
| Python 后端 | 1-2 人 | FastAPI 开发 + python-docx 渲染 + Milvus 对接 |
| Amis 管理台 | 1 人（可兼） | JSON 配置管理页面 + CRUD 接口对接 |
| 前端 | 1 人 | Vue 3 用户交互页面（对话/编辑器嵌入） |
| AI/LLM 工程师 | 1 人 | Prompt 设计 + RAG 调优（Python 原生生态） |
| 测试 | 1 人（可兼） | E2E 测试 + AI 输出质量评估 |

---

## 8. 可行性结论

### 总体判断

**✅ 项目可行，建议进入实施阶段。**

### 各模块可行性

| 模块 | 可行性 | 建议 |
|------|--------|------|
| M1 技术问答 | ✅ **高** | 优先级最高，建议首个启动 |
| M2 需求梳理 | ✅ **高** | 实施难度最低，快速见效 |
| M3 方案生成 | ✅ **中等** | 核心能力，但 PPT 渲染有技术挑战 |
| M4 标书撰写 | ✅ **中等** | 业务价值高，但招标解析需重点投入 |
| M5 政策打通 | ✅ **高** | 技术上可行，运营维护是长期课题 |

### 关键成功因素

1. **数据先行** — 技术问答和标书撰写的质量直接取决于知识库的丰富程度，客户 FA 团队需在项目启动前准备基础文档
2. **LLM API 选型** — 建议早确定服务商，API 的稳定性直接影响所有模块
3. **Milvus 部署** — 向量数据库是技术底座，建议第一批搭建
4. **模板标准化** — 模板占位符规范需要提前定义，避免后期返工

### 下一阶段建议

1. 确认本可行性研究报告内容
2. 与客户沟通确认技术选型和分批计划
3. 启动 Milvus + LLM API 的 POC（技术验证）
4. 开始 FA 案例文档的整理和向量化数据准备
5. 进入正式开发阶段

---

## 9. Demo 验证与工程实践发现

Demo 阶段（2026-06-06）搭建了前后端分离原型，覆盖 M1 技术问答和 M3 方案生成两条核心链路。开发过程中暴露了以下工程问题，**正式开发阶段必须参考这些发现**。

### 9.1 AI 输出质量控制

**问题：** LLM 生成的文档内容中夹杂大量"元话语"——
"好的，作为您的售前方案专家，我将基于前文的【整体解决方案架构】，为您撰写【系统功能规划】章节。本章将深入拆解..."

**根因：** Prompt 中写了"你是售前方案专家。请撰写..."，LLM 把角色扮演指令和任务描述当作输出格式的一部分，导致每个章节开头都是相同的模板化措辞。

**解决方案（双重保险）：**

| 层级 | 方法 | 说明 |
|------|------|------|
| **Prompt 层** | 在 Prompt 末尾加 `⚠️ 严禁事项` 列表 | 明确禁止"好的"、"作为XX专家"、"根据前文"、"本章将"等元话语 |
| **后端层** | `stripAiPreamble()` 正则后处理 | 对 LLM 输出做安全兜底，用正则剔除常见 AI 开头语（无论 Prompt 怎么调） |

```text
// 后端兜底 — 永远生效，不依赖 Prompt 质量（Java Demo 示例代码）
private String stripAiPreamble(String text) {
    return text
        .replaceFirst("(?i)^好的[，,].*?[,，].*?撰写.*?章节[。\\s]*", "")
        .replaceFirst("(?i)^作为(一名)?售前.*?[,，].*?[。\\s]*", "")
        .replaceFirst("(?i)^根据(前文|您).*?[,，].*?(?:本章|本节).*?[。\\s]*", "")
        .strip();
}
```

**教训：** Prompt 调优永远做不到 100% 可靠。所有 LLM 输出路径都需要后端兜底校验/清洗。

### 9.2 中文编码与环境兼容性

| 问题 | 现象 | 解决方案 |
|------|------|---------|
| Git Bash curl 中文 JSON 乱码 | `Invalid UTF-8 middle byte` 错误 | PowerShell `Invoke-RestMethod` 替代；或前端表单提交绕过 curl |
| Maven 编译中文报错乱码 | 错误信息在 GBK 终端不可读 | 用 `-q` 静默模式 + 重定向到文件查看 |
| Python print emoji 在 Windows 终端崩溃 | `UnicodeEncodeError: 'gbk' codec` | 只用 ASCII 字符做测试输出标记 |
| OpenCV 不支持中文路径 | `imread_()` 返回 `None` | EasyOCR 用 PIL 先读图片再转成绝对英文路径的 PNG |

**教训：** 所有终端工具链（curl / Python / Maven 日志）在 Windows 中文环境下都要做编码兼容处理。正式开发阶段建议统一用 Git Bash + UTF-8 locale。

### 9.3 Java Demo 环境兼容性（注：适用于 Java 版 Demo，Python 栈无此问题）

**问题：** Java Demo 阶段系统默认 Java 8，Spring Boot 3.x 要求 Java 17+。

**解决：**
- sdkman 安装 `java 17.0.9-tem`，每次 `mvn` 命令前 `export JAVA_HOME`
- 生成 Maven Wrapper（`mvnw`），让 Windows CMD/PowerShell 用户也能直接运行（Java Demo 特有；Python 栈无此问题）

```bash
mvn wrapper:wrapper -Dmaven=3.9.14   # 一次生成，永久可用
```

### 9.4 Vue 3 前端工程注意事项

| 问题 | 影响 | 解决 |
|------|------|------|
| `import type { X }` 与 `import X` 同名冲突 | Vue SFC 编译报错 `different imports aliased to same local name` | type import 改别名：`import type { ChatMessage as ChatMessageType }` |
| Element Plus 组件在 Vitest 中未注册 | `Failed to resolve component: el-input` | `mount()` 的 `global.plugins` 中注册 `ElementPlus` |
| `@MethodSource` 工厂方法必须 static | 参数化测试加载 eval JSON 失败 | 工厂方法加 `static`，内部自己读文件，不依赖 `@BeforeEach` |

### 9.5 内存 RAG 检索的局限性

Demo 使用内存 TF-IDF 模拟向量检索。实际中发现：

- **中文分词过粗** → 需要字符级 + 词级混合 tokenizer，长词权重高于单字
- **阈值调优困难** → 同一阈值对"制造业ERP"（精确匹配）和"社保基数"（零匹配应该返回空）难以同时满足
- **结论：** 内存版只能验证接口设计。正式开发须第一时间接入 Milvus，不要试图优化内存检索的准确率。

### 9.6 前后端联调注意事项

| 问题 | 解决 |
|------|------|
| 后端端口 8080 被残留进程占用 | `netstat -ano | grep 8080` → `taskkill` |
| 前端跨域问题 | Vite proxy 配置 `/api` 转发到 `localhost:8080` |
| API Key 注入方式 | Demo 用环境变量；正式开发应存入配置中心或数据库（FastAPI 可用 pydantic-settings） |
| 方案生成耗时 2-3 分钟 | 逐节 LLM 调用串行；正式开发应加 SSE 流式推送进度 |

### 9.7 LLM 输出截断问题

**问题：** 方案生成的 Word 文档中，章节内容在句子中间突然中断——"制定三级应急预案，覆盖从轻微故障到重大灾难的场景：- **一级预案（局部故障）**：针对单一模块（如采购"——明显下方还有大量内容未输出。

**根因：** `LlmRequest.maxTokens` 设置为 1200，LLM 在达到 token 上限后强制截断输出，不管内容是否完整。

**解决方案：**

| 项目 | Before | After |
|------|--------|-------|
| maxTokens | 1200（约 800 中文字） | 3000（约 2000 中文字） |
| 实际输出 | 每节 800-1500 字，经常截断 | 每节 3000-4200 字，完整收尾 |

**教训：** 方案/标书这类内容生成场景，maxTokens 必须留足冗余。每节至少需要 2000-3000 字的输出空间。正式开发时建议：
- 根据章节类型动态调整 maxTokens（背景分析 1000、方案设计 3000、实施计划 2000）
- 增加输出完整性检查：如果 LLM 返回的 `finish_reason` 是 `length`（被截断），自动用更大的 maxTokens 重新请求

### 9.8 Word 文档格式丢失问题

**问题：** LLM 生成的内容包含丰富的 Markdown 格式（`###` 标题、`**` 加粗、`-` 列表项），但下载的 Word 文档中所有内容挤在一大段里，没有分段、没有标题层级、没有加粗——完全不可用。

**根因：** Demo 初版的 `generateDocx()` 方法把整节内容当纯文本写入一个 `XWPFParagraph`，完全忽略了 LLM 输出的 Markdown 标记。

**解决方案：** 实现了一个轻量 Markdown → Word 渲染器：

```text
// 逐行解析 Markdown（Java Demo POI 示例，Python 栈等效方案见 python-docx + lxml）
private void renderMarkdownContent(XWPFDocument doc, String text) {
    for (String line : text.split("\n")) {
        // ## → Word 14pt 加粗标题
        // ### → Word 13pt 加粗标题
        // - → 缩进 400 + 项目符号
        // 1. → 缩进有序列表
        // **text** → 内联加粗（split 后交替 bold）
    }
}
```

| Markdown | Word 效果 |
|----------|----------|
| `# 一级标题` | 16pt 蓝色加粗 |
| `## 二级标题` | 14pt 加粗 |
| `### 三级标题` | 13pt 加粗 |
| `#### 四级标题` | 12pt 加粗 |
| `##### 五级标题` | 11pt 加粗 |
| `**加粗文字**` | 内联 Bold |
| `- 列表项` | 缩进 + `•` 项目符号 |
| `1. 有序列表` | 缩进 400 |

**教训：**
1. **LLM 天然输出 Markdown**，不要试图让 LLM "别用 Markdown"（会降低输出质量）。正确做法是解析 Markdown。
2. 正式开发阶段需要更完整的 Markdown → Word 渲染器，至少需要支持：表格（`|` 语法）、代码块、多级嵌套列表、链接、图片占位符。
3. PPT 生成同理——需要定义 Markdown 到 PPT 幻灯片结构的映射规则（`##` → 新幻灯片标题、`-` → 正文要点）。
4. 方案/标书这类文档，**格式质量 = 产品可用性**。纯文本输出的方案客户不会用，必须做到开箱即用的专业排版。

### 9.9 模板填充的格式保留问题

Demo 实现了"基于已有 Word 模板 + AI 填充内容"的功能（`POST /api/v1/bid/fill`），开发过程中暴露了三个核心问题。

**问题一：新建文档丢失原始样式**

**现象：** Java Demo 用 Apache POI 创建新文档并逐元素复制原模板，下载的 docx 中所有样式丢失——`Heading 3` → `Normal`、`Body Text First Indent 2` → `Normal`。（Python 栈用 python-docx + lxml 可避免此问题，但模板操作仍需谨慎。）

**根因：** `XWPFDocument` 的样式表（styles.xml）不会随段落复制而自动迁移，需要在复制时显式克隆样式定义。

**解决方案：** Demo 改用 **Python `fill_template.py` 子进程**方案——通过 `lxml` 直接在原模板的 XML 层面插入新段落，原模板样式表完全不受影响。

```
调用路径（Java Demo 子进程方案）:
  BidTemplateService.generateFilledDocx()
    → 写原模板 + AI内容JSON到临时文件
    → ProcessBuilder("python", "fill_template.py", ...)
    → Python用lxml在原模板XML中插入AI段落
    → 返回填充后的docx字节

> 注：Python 栈可直接调用 fill_template.py，无需 Java 子进程。
```

**教训：** POI 适合"从零创建文档"，不适合"在保留格式的前提下修改已有文档"。正式开发阶段需要：
- 封装一个 `DocxTemplateEngine`，底层用 `lxml` / `python-docx` 或 POI 的 `CTBody` XML 操作
- 或者引入 Aspose.Words（商业库）原生支持模板操作
- 所有段落级操作（插入/替换/删除）必须保留原样式 ID 引用

**问题二：模板章节自动检测不可靠**

**现象：** `extractSections()` 方法依赖 `style.startsWith("Heading")` 检测章节标题，但 POI 返回的样式名可能与 Word 中显示的不同（如中文模板中显示 "Heading 3" 但 POI 返回其他值）。

**根因：** docx 内部样式存储用 styleId（如 `3`）而非显示名，不同版本的 POI 对样式名的映射行为不一致。

**解决方案：** 增加多层 fallback 检测机制：

| 检测层 | 匹配规则 | 示例 |
|--------|---------|------|
| 样式名 | `style.toLowerCase().contains("heading")` | "Heading 3" |
| 中文序号 | `text.matches("^[一二三四五]、.+")` + 加粗 | "一、收费公式" |
| 语义特征 | `text.endsWith("表")` / `text.contains("方案")` + 加粗 | "报价一览表" |

**教训：** docx 章节结构检测不能只靠样式名。正式开发需要：
- 同时检测样式 ID（`pPr.pStyle.val`）和样式名
- 支持手动标注（用户在管理端标记哪些段落是"章节标题"）
- 对检测结果做置信度打分，低置信度 → 提示用户确认

**问题三：Markdown 表格无法填充到模板表格中**

**现象：** AI 生成的内容中包含 Markdown 表格（`| 序号 | 费用 |`），但在模板中应填充到原模板已有的 Word 表格中，而非作为新表格插入。

**当前做法：** AI 生成的 Markdown 表格被转换为新 Word 表格，插入在章节标题后。可以工作，但**与原始模板表格是两张不同的表**——原始模板的空白表格仍然保留，导致文档中出现重复表格。

**理想做法：**
1. 解析原模板中已有的表格结构（行数、列数、表头）
2. 将 AI 生成的表格数据映射到原模板表格的对应单元格
3. 如果 AI 行数 > 模板行数，动态追加行

**教训：** 这是 Demo 未解决的核心问题。正式开发需要实现"表格映射填充"——不仅仅是 Markdown→Word 表格转换，而是将 AI 数据精确填入原模板的指定表格位置。这需要：
- 为模板中的每个表格定义 `tableId` 和 AI 填充规则
- AI 输出结构化 JSON 而非 Markdown 表格（`[{"序号":1, "费用":"软件许可", ...}]`）
- 后端按行列映射填入原模板表格

### 9.10 浏览器端在线编辑集成

Demo 集成了 [ranuts/document](https://github.com/ranuts/document)（基于 OnlyOffice 的纯浏览器端编辑器），实现了"AI 生成 → 在线预览编辑 → 下载"的完整闭环，无需用户下载后才能看到效果。

**问题：URL 方式加载文件失败**

**现象：** 最初尝试通过 `?file=<URL>` 参数让编辑器加载后端返回的 docx，报错 `Unsupported file format`。

**根因：** 编辑器通过 URL 路径中的文件扩展名或响应 Content-Type 判断格式。后端 GET 接口路径为 `/api/v1/bid/file/{fileId}`，不含 `.docx` 后缀，编辑器无法识别。

**解决方案：** 改用编辑器的 **postMessage API** + Embed 模式（`?embed=1`）。父窗口通过 `postMessage` 将 docx 二进制数据（`Uint8Array`）直接推送给 iframe，不依赖 URL 路径。

```
集成架构:

┌──────────────────────────────────┐
│  父窗口 (Vue 3, :5173)          │
│  ┌────────────────────────────┐ │
│  │ ① AI 生成 docx → base64    │ │
│  │ ② base64 → Uint8Array      │ │
│  │ ③ postMessage ──────────┐  │ │
│  └──────────────────────────│──┘ │
│                              │    │
│  ┌──────────────────────────│──┐ │
│  │ iframe (:3000/?embed=1)  │  │ │
│  │                          ▼  │ │
│  │ ④ document:open-buffer    │ │ │
│  │    {buffer, fileName}     │ │ │
│  │ ⑤ 用户编辑 Word           │ │ │
│  │ ⑥ document:saved → Blob  │ │ │
│  └────────────────────────────┘ │
│  ⑦ 触发浏览器下载               │
└──────────────────────────────────┘
```

**关键 message 类型：**

| 方向 | type | payload | 作用 |
|------|------|---------|------|
| iframe→父 | `document:ready` | — | 编辑器初始化完成 |
| 父→iframe | `document:open-buffer` | `{buffer: Uint8Array, fileName: string}` | 传入二进制文档数据 |
| iframe→父 | `document:saved` | `{file: Blob, fileName: string}` | 用户保存，返回编辑后的文件 |

**教训：**
1. **推荐架构是"父系统管文件，iframe 只管编辑"**——父系统负责 AI 生成/文件存储/鉴权，编辑器不碰后端 API，通过 postMessage 收文件、回传保存结果。这避免了 CORS、鉴权、格式检测等问题。
2. 正式开发阶段应将编辑器静态文件集成到前端项目中（同域部署），消除跨域限制，并可深度定制编辑器 UI。
3. `document:open-buffer` 方式比 URL 方式更隐私安全——文件数据不出浏览器，符合客户对数据安全的关注。
4. 当前 Demo 编辑器跑在独立端口（:3000），后续应将其静态文件直接放入 Vue 项目的 `public/` 目录或作为独立路由。

### 9.11 iframe 嵌入第三方服务的同域最佳实践

**问题：** 文档编辑器起初作为独立服务运行在 `localhost:3000`，前端 iframe 跨域加载（`src="http://localhost:3000/?embed=1"`），导致 postMessage 通信失败——浏览器安全策略要求 `event.origin` 精确匹配，跨域时 `event.origin !== window.location.origin`，所有 message 被丢弃。

**根因：** 增量开发中先独立验证编辑器功能（最快路径），之后未及时将独立服务通过代理收敛到前端同域。

**解决方案：** Vite dev server 的 proxy 能力不仅适用于后端 API，同样适用于任何需要同域访问的第三方服务。

```typescript
// vite.config.ts — 一条规则收敛三个服务到同域
proxy: {
  '/api':    { target: 'http://localhost:8080', changeOrigin: true },
  '/editor': { target: 'http://localhost:3000', changeOrigin: true,
               rewrite: (p) => p.replace(/^\/editor/, '') }
}
```

修复后架构：

```
浏览器只访问 http://localhost:5173
  ├── /                → Vue 3 前端
  ├── /api/*           → FastAPI (8080)
  └── /editor/*        → OnlyOffice 文档编辑器 (3000)

iframe src="/editor/?embed=1"  ← 同域，零跨域问题
EDITOR_ORIGIN = window.location.origin  ← postMessage 同域通信
```

**效果对比：**

| | 修复前 | 修复后 |
|---|---|---|
| 用户访问端口 | 5173 + 3000（两个） | 5173（一个） |
| iframe 域 | 跨域（localhost:3000） | 同域（/editor/） |
| postMessage | ❌ origin 不匹配 | ✅ 同域直接通信 |
| CORS 配置 | 需要后端配 CORS | 不需要（同域） |
| 部署复杂度 | 两个独立服务 | 生产环境可合并为一个 Nginx 站点 |

**教训：**
1. **iframe 嵌入第三方服务的第一反应应该是 Vite proxy → 同域**，而不是跨域 postMessage。同域方案零 CORS 问题、零 origin 校验。
2. 增量开发中的"临时独立服务"需要在架构 checklist 中标记为"待收敛"，避免 Demo 结束后遗留。
3. 生产部署时的等效方案：Nginx `location /editor/ { proxy_pass http://editor:3000/; }`——完全相同的模式。
4. 最终目标：将编辑器的静态文件构建产物直接放入 Vue 项目的 `public/editor/` 目录，彻底消除独立服务进程。

---

## 10. 多角色评审：不足与改进

本章从安全架构师、运维 SRE、产品经理、数据工程师、成本财务五个视角，对可行性研究报告进行系统性审视，指出缺失项并给出改进建议。

### 10.1 安全视角

**缺失 1：LLM API Key 和凭证管理方案（严重度：🔴 高）**

文档提到用第三方大模型 API，但完全没有涉及 API Key 的存储、轮换、权限隔离。Key 是明文存配置文件还是加密存储？多人团队谁有权查看？

**建议：** 补充到 2.3 关键架构决策。
- API Key 存入配置中心加密字段（FastAPI 可用 pydantic-settings + 环境变量），禁止写入配置文件
- 支持 Key 轮换（同时保留新旧两把 Key，切换期间零中断）
- 操作审计：谁在何时使用了哪把 Key

**缺失 2：用户数据经过 LLM 的隐私边界（严重度：🔴 高）**

FA 在技术问答中输入的内容可能包含客户名称、合同金额等敏感信息。这些数据会原样发送给 DeepSeek/Qwen 的服务器。

**建议：** 补充到 6 风险矩阵。
- 是否需要前端脱敏层（自动识别并替换敏感字段）？
- 是否需要在发送前弹窗提示"以下内容将发送给第三方 AI 服务"？
- 客户是否接受项目数据经过公有云 LLM？有没有合同条款约束？

**缺失 3：NL2SQL 的租户数据隔离（严重度：🔴 高）**

Demo 做了 SQL 关键字拦截（只读），但文档没有明确"用户 A 查合同，绝不能查到用户 B 的合同"的硬约束如何保证。

**建议：** 补充到 3.1 技术问答。
- LLM 生成 SQL 后，后端自动追加 `WHERE tenant_id = ?`（从当前登录 Session 取）
- 双重保障：数据库连接本身也限制为只读账号 + 行级安全策略

---

### 10.2 运维 SRE 视角

**缺失 4：LLM API 的 SLA 和降级策略缺乏量化（严重度：🟡 中）**

文档提到"多模型切换+降级策略"，但没有量化的 SLA 目标。

**建议：** 补充到 2.3。
- 主模型不可用 → 自动切换到备用模型（延迟 < 2s）
- LLM 全部不可用 → 降级返回检索原文（功能降级但不中断）
- SLA 目标：AI 功能可用率 ≥ 99.5%（月度）
- 对应架构：需要熔断器（连续失败 N 次自动切换）、健康检查心跳

**缺失 5：Milvus 向量库的高可用方案（严重度：🟡 中）**

文档说 Milvus 独立部署，但未提及单点故障风险。Milvus 挂了 → M1/M3/M4 全部瘫痪。

**建议：** 补充到 6 风险矩阵。
- 生产环境至少 Milvus Standalone + 定期备份
- 或者使用 Milvus Cloud / Zilliz Cloud 托管服务（含 SLA）
- 降级方案：Milvus 不可用时，回退到 MySQL 全文索引（精度降低但可用）

**缺失 6：可观测性体系空白（严重度：🟡 中）**

整个文档没有提到日志/监控/告警。

**建议：** 新增 2.7 节"可观测性设计"。
- LLM 调用成功率、P50/P99 延迟、Token 消耗趋势 → Prometheus + Grafana
- 用户问答满意度反馈机制（👍/👎 按钮 + 可选文字反馈）
- 异常告警规则：API 错误率 > 5%、Token 日消耗超过预算 150%、Milvus 查询延迟 > 1s

---

### 10.3 产品经理视角

**缺失 7：M2→M3 缺少端到端数据流图（严重度：🟡 中）**

M2（需求梳理）输出结构化 JSON，M3（方案生成）以此为输入。但从"对话记录"到"结构化 JSON"再到"方案各章节"，链路很长，客户难以直观理解。

**建议：** 在 3.2 和 3.3 之间插入一幅端到端数据流图。

```
用户对话 → 需求结构化JSON → 方案大纲规划 → 逐节生成 → 模板填充 → 下载
  │            │                │            │          │
  └─ 追问状态机  └─ 固定+灵活字段  └─ LLM规划   └─ LLM撰写  └─ python-docx
```

**缺失 8：用户权限模型缺失（严重度：🟡 中）**

五个模块面向不同角色？文档未定义角色-权限矩阵。

**建议：** 新增章节。
| 角色 | 技术问答 | 需求梳理 | 方案生成 | 标书撰写 | Prompt管理 | 政策审核 | 模板管理 |
|------|---------|---------|---------|---------|-----------|---------|---------|
| FA 专员 | ✅ | ✅ | ✅ | ✅ | ❌ | ❌ | ❌ |
| FA 主管 | ✅ | ✅ | ✅ | ✅ | ❌ | ✅ | ✅ |
| 管理员 | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |

**缺失 9：需求梳理会话持久化（严重度：🟢 低）**

文档讨论了追问状态机，但没有提到用户关掉浏览器后能否恢复。这在销售场景常见——FA 和客户聊到一半，下次继续。

**建议：** 补充到 3.2。
- Session 状态自动持久化到 Redis（30 天 TTL）
- 用户重新打开 → 自动恢复到上次中断的维度
- "新建需求梳理" vs "继续上次"的入口设计

---

### 10.4 数据工程师视角

**缺失 10：知识库版本管理和过期策略（严重度：🟡 中）**

FA 案例库持续新增/更新。一篇案例更新后，向量库中的旧版本如何失效？政策有过期管理，但产品文档和案例库也需要版本控制。

**建议：** 补充到 3.1 前提条件节。
- 每篇文档带 `version` 和 `status`（draft/published/archived）
- 向量化时只索引 `status=published` 的文档
- 文档更新 → 重新向量化 → 新向量替换旧向量（同 docId）
- 软删除：标记 archived，不物理删除（可追溯）

**缺失 11：Embedding 模型选型未定（严重度：🔴 高）**

文档提到用 LLM API 的 Embedding，但没有明确用哪个模型。Embedding 模型直接决定检索质量，且更换模型需要全量重新向量化——成本极高。

**建议：** 补充到 4 技术选型汇总。
| 方案 | 模型 | 维度 | 中文效果 | 成本 |
|------|------|------|---------|------|
| DeepSeek Embedding | deepseek-embedding | 1536 | 中等 | API 调用计费 |
| BGE-M3 | BAAI/bge-m3 | 1024 | 优秀 | 需自部署 GPU |
| 推荐 | **BGE-M3**（Demo 期可用 DeepSeek 快速验证） | — | — | — |

> 注：Embedding 模型一旦选定，后续更换需全量重新向量化所有文档。建议在 Demo 阶段对比 2-3 个模型的检索效果后再锁定。

---

### 10.5 成本/财务视角

**缺失 12：LLM Token 成本估算完全空白（严重度：🔴 高）**

客户一定会问"AI 功能每个月要花多少钱"。文档完全没有涉及。

**建议：** 补充到 7 资源估算，新增 7.4 节"AI 运营成本估算"。

| 场景 | 预估月调用量 | 单次平均 Token | 月 Token 消耗 | 预估月费 |
|------|------------|--------------|-------------|---------|
| 技术问答 | 500 次/月 | 2,000 | 1,000,000 | ~¥140 |
| 需求梳理 | 100 次/月 | 8,000 | 800,000 | ~¥112 |
| 方案生成 | 50 次/月 | 30,000 | 1,500,000 | ~¥210 |
| 标书撰写 | 20 次/月 | 50,000 | 1,000,000 | ~¥140 |
| 政策抓取解析 | 4 次/月 | 10,000 | 40,000 | ~¥6 |
| Embedding | 200 次/月 | 500 | 100,000 | ~¥2 |
| **合计** | — | — | **~4,440,000** | **~¥610/月** |

> 按 DeepSeek V3 价格估算：输入 ¥1/M token，输出 ¥2/M token。实际费用按调用量浮动 ±30%。

**缺失 13：API vs 私有化部署的长期成本 break-even（严重度：🟡 中）**

文档选择了 API 模式，但没有对比私有化部署的成本拐点。

**建议：** 补充到 7.4。

| 模式 | 年成本 | 适用月调用量 |
|------|--------|------------|
| 纯 API（DeepSeek） | ~¥7,320/年 | < 10M token/月 |
| 私有化部署（1×A100 GPU 服务器） | ~¥80,000/年（含硬件+运维+电费） | > 50M token/月 |
| 混合（API 日常 + 私有化峰值） | 按比例 | 10M-50M token/月 |

> break-even 约在月消耗 30M-40M token。初期（Q1-Q2）用 API 模式，Q3 根据实际用量数据决定是否上私有化。

---

### 10.6 改进行动项汇总

| # | 类别 | 缺失项 | 严重度 | 补充位置 | 建议动作 |
|---|------|--------|--------|---------|---------|
| 1 | 安全 | API Key 凭证管理 | 🔴 | 2.3 | 补充加密存储+轮换方案 |
| 2 | 安全 | 用户数据经 LLM 的隐私 | 🔴 | 6 | 加入风险矩阵，补充脱敏+告知方案 |
| 3 | 安全 | NL2SQL 租户数据隔离 | 🔴 | 3.1 | 补充 WHERE tenant_id 自动注入 |
| 4 | 运维 | LLM SLA 与降级量化 | 🟡 | 2.3 | 补充熔断器+健康检查+99.5% SLA |
| 5 | 运维 | Milvus 高可用 | 🟡 | 6 | 加入风险矩阵，补充降级回退 MySQL |
| 6 | 运维 | 可观测性体系 | 🟡 | 新增 2.7 | Prometheus+Grafana+告警规则 |
| 7 | 产品 | M2→M3 数据流图 | 🟡 | 3.2-3.3 衔接 | 插入端到端数据流图 |
| 8 | 产品 | 用户权限模型 | 🟡 | 新增 | 角色-权限矩阵 |
| 9 | 产品 | 需求梳理会话持久化 | 🟢 | 3.2 | 补充 Redis 持久化+恢复方案 |
| 10 | 数据 | 知识库版本管理 | 🟡 | 3.1 | 补充文档 version/status 生命周期 |
| 11 | 数据 | Embedding 模型选型 | 🔴 | 4 | 确定 BGE-M3 vs DeepSeek Embedding |
| 12 | 成本 | Token 成本估算 | 🔴 | 7.4 新增 | 月成本 ~¥610，含分场景明细 |
| 13 | 成本 | API vs 私有化 break-even | 🟡 | 7.4 新增 | break-even ~30M-40M token/月 |

---

> **文档变更记录**

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|---------|------|
| v1.0 | 2026-06-05 | 初稿 | AI 辅助生成 |
| v1.1 | 2026-06-06 | 新增第 9 节：Demo 验证与工程实践发现 | AI 辅助生成 |
| v1.2 | 2026-06-07 | 新增 9.7-9.11 节、第 10 节多角色评审 | AI 辅助生成 |
| v1.3 | 2026-06-07 | 技术栈更新：Yudao(Java) → FastAPI-Amis-Admin(Python) | AI 辅助生成 |

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|---------|------|
| v1.0 | 2026-06-05 | 初稿 | AI 辅助生成 |
| v1.1 | 2026-06-06 | 新增第 9 节：Demo 验证与工程实践发现 | AI 辅助生成 |
| v1.2 | 2026-06-07 | 新增 9.7-9.10 节：输出截断、格式丢失、模板填充、在线编辑集成 | AI 辅助生成 |
|------|------|---------|------|
| v1.0 | 2026-06-05 | 初稿 | AI 辅助生成 |
| v1.1 | 2026-06-06 | 新增第 9 节：Demo 验证与工程实践发现 | AI 辅助生成 |
>
> | 版本 | 日期 | 变更内容 | 作者 |
> |------|------|---------|------|
> | v1.0 | 2026-06-05 | 初稿 | AI 辅助生成 |
