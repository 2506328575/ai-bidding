FROM node:20-alpine AS build
WORKDIR /app
COPY ai-bidding-frontend/package*.json .
RUN npm ci
COPY ai-bidding-frontend/ .
RUN npm run build

FROM nginx:alpine
COPY deploy/nginx.conf /etc/nginx/nginx.conf
COPY --from=build /app/dist /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
